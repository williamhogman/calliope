"""Calliope — generated world simulation.

The original package bootstrapped Hy here to load the .hy modules; those
files remain as historical reference, but the live simulation is pure
Python now, so no import hook is needed.
"""
