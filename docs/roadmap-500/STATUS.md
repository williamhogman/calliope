# The Five Hundred — Status Ledger

Landed phases of `../ROADMAP-500.md`, one row per phase, appended when
the phase's gate runs green in the diagnostics harness. The specs in
the era files are binding; this ledger only records completion.

| Phase | Title | Landed | Gate evidence |
|---|---|---|---|
| M16 | Plates Remembered | 2026-08-18 | terrain lane: plate count/mean drift-age banded, sketch+heightmap regen byte-identical ×3 seeds; full suite 381 pass · 14 warn · 0 fail (new warns = host ~1.8× slower across untouched stages — perf.txt vs c4c51c0; assay law fixed: phantom baseline on unlisted goods) |
| M17 | Orogeny Ages | 2026-08-18 | terrain lane: young/old belt sharpness split banded per seed; rock ledger in `hash_state` |
| M18 | Rock Provinces | 2026-08-18 | terrain+resources lanes: province shares banded, ore placement biased by province (χ² check green) |
| M19 | Ore Follows Rock | 2026-08-18 | resources lane: per-kind province affinity holds ×3 seeds; floor guarantees intact (ADR-0013) |
| M20 | Karst and Caves | 2026-08-18 | terrain lane: karst share of carbonate belt banded; spring-fed streams counted in hydro lane |
| M21 | Soil Provinces | 2026-08-18 | terrain lane: soil-class shares banded; fertility correlation with class sign-correct ×3 seeds |
| M22 | Fault Seams and Earthquakes | 2026-08-18 | seismic ledger byte-identical native↔wasm (ADR-0025); quake magnitude–frequency slope banded |
| M23 | Live Volcanism | 2026-08-18 | civ lane: eruption cadence + VEI distribution banded; ash fertility bonus measured on flanks |
| M24 | Disaster Wiring | 2026-08-18 | civ lane: fell/rebuild arcs close inside forty years (recovery warn is pre-staged calibration); ruins persist in chronicle |
| M25 | Sea-Level History | 2026-08-19 | earth lane: eustatic curve + isostatic response IEEE-exact, in `earth_hash` replay identity |
| M26 | Drowned and Raised Coasts | 2026-08-19 | terrain lane: ria/skerry/raised-beach counts banded; raised beaches thicken with the rise ×5 seeds |
| M27 | Deep-Earth Determinism | 2026-08-19 | earth-wasm lane: plates·rock·seismic·volcanism·sealevel·landform one identity native↔wasm, seed 777 × 240 mo |
| M28 | Ice-Sheet Extent | 2026-08-19 | earth lane: ELA mass-balance extent banded (ice 39–41% of land at LGM, margins ~60°); extent grid in `hash_state` |
| M29 | Ice-Carved Relief | 2026-08-19 | terrain lane: U-valley/cirque/hang counts banded per glaciated belt; fjords classified + named; height NaN-free |
| M30 | Depositional Legacy | 2026-08-19 | terrain lane: till/moraine/drumlin/esker + loess-mantle bands green ×6 seeds; counterfactual legacy uplift +0.039–0.054 on the farmable belt (≥ +0.005 gate); civ lane: footprint towns +0.039–0.051 vs zeroed deposits; full suite 483 pass · 13 warn · 0 fail, replay identity holds with loess in the ice ledger |
| M31 | Proglacial Lakes and Spillways | 2026-08-19 | terrain lane: 5–16 moraine-dammed lakes ×3 seeds (≥64 km², shore-rim dam test), spillways drain downhill (0 ascending steps ×3 seeds), width bands scale with catchment (wide-class basins 46–210 cells vs 6–16); chains counted via union-find; lakes+spillways+meta in the ice ledger → replay identity native↔wasm; full suite 495 pass · 12 warn · 0 fail |
| M32 | Outwash Plains and Braided Meltwater Rivers | 2026-08-19 | terrain lane: 853–1831 outwash cells ×4 seeds (meltwater routed off the sheets, corridors + planed aprons), braided share of below-margin rivers 3.1–5.5% (banded 2–35 sweet), counterfactual fertility uplift +0.027–0.033 on the farmable plain (banded); outwash grid in the ice ledger + BRAIDED CellFlags bit on the wire; full suite 503 pass · 14 warn · 0 fail (both extra warns pre-existing: pack 21.0 B/cell and payload 7238 B flat across six bench-history rows), replay identity native↔wasm holds |
| M33 | Permafrost and Patterned Ground | 2026-08-19 | earth lane: extent 25.9–29.5% of land ×3 seeds (banded), patterned ground on 65.6–74.1% of the real-permafrost belt (polygons on flats, stripes on slopes), maritime frontier median −1.80..−1.88 °C (gate: tracks the −2° isotherm ±0.75 where the continental shift vanishes), continental frontier +2.12..+2.34 °C warmer (Siberia asymmetry sign-correct), Jaccard vs the isotherm 87.7–90.1%; PERMAFROST+PATTERNED CellFlags bits on the wire at zero pack cost, PATTERNED joins the landform vocabulary, permafrost hash joins hash_state and the deep-earth identity line (agrees native↔wasm); inspector explains frozen ground per cell; full suite 509 pass · 13 warn · 0 fail |
| M34 | Mountain Glaciers | 2026-08-19 | earth lane: modern ice from a PDD mass balance (freezing-month snowfall − 120 mm/°C·mo melt, libm-free via the exact COS12 table) on the shipped climate grids; share 12.4–15.4% of land ×3 seeds (polar caps + alpine, banded 3–18 sweet), alpine glaciers sit +140..+270 m above the belt snowline solved from belt-mean climate (cap belts excluded from the law), 100% nested inside the LGM footprint (the colder-world invariant, banded 70–100); balance grid `ice.modern` joins Ice::hash → hash_state + the deep-earth identity line (agrees native↔wasm), GLACIER ships as the eighth and last CellFlags bit, inspector names permanent ice; full suite 511 pass · 14 warn · 0 fail (14th is stage-naming 123 ms vs 120 sweet — timing flicker, naming untouched) |
| M35 | Glacier-Fed Discharge | 2026-08-19 | hydro lane: glacier melt runs a second discharge lane (snowfall on ice partitioned into `melt_throughput` — annual melt, summer-phased amplitude via COS12, rain carry-through), accumulated down the same D8 tree and folded into a combined seasonal `flow_amp`; glacier-fed rivers (≥25% melt share) 1.3–1.5% of river cells ×3 seeds (banded 0.5–15 sweet), median melt share 30.8–48.4%, 12-month reconstructed discharge curves non-negative and NaN-free on every river cell, 100% of glacier-fed reaches peak in warm months (≥80% gate), 0 glacier-fed wadis (the wadi stamp yields to the melt); melt+melt_amp grids join Ice::hash → hash_state + deep-earth identity (agrees native↔wasm, ice=33baf87e8e427bb3 seed 777); full suite 521 pass · 16 warn · 0 fail (extra warns are history flicker on the people axis — assimilation/civ cadence — plus the standing perf/pack set; one suite-run properties truncation was an I/O fluke, lane reruns clean at 42 pass) |
| M36 | Ice Diagnostics | 2026-08-19 | earth lane, five new ice-cadence rows ×3 seeds: fjord density 4.9–56.4 per 1000 polar-coast cells (≥55°, banded 3–150 sweet), fjord latitude discipline 100% poleward of 45° (Earth's fjord coasts run 42–83°), proglacial-lake cadence 0–0.16 per 1000 iced cells in the 50–75° margin belt (seed 777 keeps its giants poleward of the belt), moraine cadence 18.9–32.0 per 1000 iced in the margin belt, lowland (h<0.30) moraine concentration 43.9–73.4% within ±6° of the measured margin — the first draft banded ALL moraines against the lowland margin and failed at 27%; the law is a lowland law, alpine moraines follow their own snowlines; diagnose-only change (no engine state touched, replay identity unmoved); earth lane 31 pass · 0 warn · 0 fail, suite 526 pass · 16 warn · 0 fail |
| M37 | Sea Ice | 2026-08-20 | climate lane ×3 seeds: SST proxy (tmean/tamp via COS12, seawater freezes at −1.8 °C with ocean seasonal damping) gives ever-frozen ocean area 14.4–16.8% cosine-weighted (raw cell share 39–42% is fat-polar-row projection bias — area weighting is the Earth-comparable number), seasonal fringe 19–23k cells, pack reaches 59° (no icebound tropics); trade grid carries a 12-bit frozen calendar per cell — perennial pack prices as wall (45.0), seasonal straits surcharge by months shut, and any frozen sea point closes the whole route for those months (`Route.closed` mask); economy lane 100y: 10 winter-closed of 125 sea-touching routes, 0 malformed masks, the ledger obeys the ice (0 mismatched route-months — flow zero when shut, alive when the water opens), route costs deterministic across reruns; sweep 100y ×5 seeds: icebound lanes 3–10 per seed, all closures hemisphere-true winter arcs; `sea_ice_masks_are_winter_arcs` joins the assay. The sweep caught a real bug: `World::widen` grew every grid except the new frozen mask, so post-widen route coordinates read a square grid — OOB panic on seed 777; margins now extend the edge column's ice calendar (open ocean at the same latitude, physically exact). Full suite 544 pass · 12 warn · 0 fail |
| M38 | Tundra Honesty | 2026-08-20 | terrain lane ×3 seeds, four new cold-rim rows all green: treeline follows a growing-season law (`climate::gdd5` off the exact COS12 table), not the annual mean — Whittaker-forest cells short of `GDD_TREELINE` demote (cold rows → tundra; MAAT ≥ 5 °C maritime rows → moorland grass, Faroe heath not Yamal), long-summer trow-1 "tundra" promotes into the cool-temperate row (the Siberian paradox); treeline−permafrost offset −15.9..−17.4° (one-signed maritime law, banded −22..−8 sweet), tracking spread 5.9–11.1° (≤12), treeline GDD discipline 523–533 °C·day (350–850), wet share of tundra 10.7–18.4% (8–60); tundra splits wet/dry on M33's `permafrost::extent_class` + flatness `WET_G` 0.010 + `WET_PMIN` 220 mm — `WetTundra` ships as biome 11 through engine, meta and client palette (the diagnose census arrays were `[0; 11]` and would have panicked on the new code; widened to 12 across the suite). Calibration honesty: GDD 600 (Paulsen–Körner ≈640) against our tame amplitudes gutted the taiga to 0.2% of land — 500 (low edge of the 500–800 literature proxies) keeps the demotion honest and the belt standing at 0.5%; the residual starvation vs the annual-mean law's 2.5% is the amplitude model's fault, staged in the Ready queue as its own phase-sized recalibration (the four forest-share WARNs are its known face). The sweep also caught a real M12 breach: `politics::union_pass` runs after the culture pass and rewrote crowns leaving void leanings (drift toward a people no longer ruling) visible at read time — all three crown-write sites (transfer, secession, union) now kill the leaning at the moment of the change unless the new banner flies the very people the town was drifting toward; seed 777 ×150y reads 0 breaches with all 21 kindred foreign-crowned towns still drifting. Full suite 548 pass · 20 warn · 0 fail (forest set staged; browser/perf rows are suite-load flicker — quiet re-run passes boot and halves the long-task row) |
| M39 | Glacial Calibration vs Earth | 2026-08-20 | earth lane, five new earth-calibration rows ×3 seeds closing Year 2 — the ice-age world held against Earth's cold-latitude bones, not just its own internal belts: fjord median latitude 67.6–68.5° (banded 52–72 sweet; Norway 58–71 / Greenland 60–83 / Chile 42–56), fjord latitude IQR 3.4–5.3° (≤20 — the carve clusters on cold coasts, not smeared over the globe), proglacial giants 6.8–21.7 per Mkm² formerly iced (Earth's LGM fringe ≈1.5–4.5 over ~34 Mkm²; our young world keeps every basin undrained — Holocene lake attrition staged below), glacier elevation-vs-latitude curve descends poleward of its crest 100% ×3 seeds (per-15°-belt mean glacier elevation, 150 m slack; seed 777 reads 45–60°:1724 m → 60–75°:625 m → 75–90°:169 m), polar-belt ice at 169–276 m (Earth's polar glaciation level 0–800 — the caps ride down to the sea); the curve check judges only the poleward limb since Earth's crest sits in the dry subtropics (~25–30°), and an equatorward rise into the crest is honest; diagnose-only change (five bands live beside the system in ice.rs, no engine state touched, replay identity unmoved — seismic 9d5b65ab252110b3 seed 12345 both chunkings); earth lane 36 pass · 0 warn · 0 fail, full suite 555 pass · 18 warn · 0 fail (warn set is the standing forest-amplitude family + perf/pack/wasm rows, two fewer than M38's run) |
| M40 | Wind-Driven Gyres | 2026-08-20 | earth lane ×3 seeds, Era II opens — the ocean circulates the way its winds already say it must: new `currents.rs` solves the depth-integrated Sverdrup balance (β·v = curl τ off the three rain-march wind bands — easterly trades to 30°, westerlies to 60°, polar easterlies beyond, as a polynomial stress profile) integrated westward from each ocean run's eastern shore, closed against the western wall by a rational Stommel boundary factor d/(d+3) (no transcendentals beyond the shared smoothing kernel — ADR-0025 discipline), knit across coastline steps by one σ=2 pass, then differentiated into surface currents u,v along the ψ contours; gyre rotation sense 6/6 basin-hemispheres earthly (M40 gate: clockwise north · counterclockwise south, judged by mean ψ over the 10–40° subtropical band per labeled ocean basin ≥2500 cells — measured, not assumed from the derivation), western boundary intensification 2.6–4.1× (p95 speed in the 4-cell western strip over the interior — the Gulf-Stream-side crowding, banded 1.5–12 sweet), surface speed p95 0.896–0.947 with SPEED_SCALE=4.0 pinning a typical world near 1 (raw ψ-gradients ran 0.237), current field replays identical across chunkings (M40 gate) and `Currents::hash` joins `hash_state`; pure derived state computed at the dawn off the final widened coastline (like landform/permafrost), never ticked; full suite 561 pass · 17 warn · 0 fail (one fewer warn than M39 — the standing forest-amplitude family + perf/pack/wasm rows, no new members) |
| M41 | Heat Transport | 2026-08-20 | terrain lane ×3 seeds, three new rows all green — water remembers the latitude it came from: `climate::current_bias` displaces each ocean cell's origin `HEAT_RES`=25 rows upstream along the meridional current (clamped ±60 rows), reads the zonal sea-surface law (28 − 53·lat^1.7) at origin minus home — poleward flow warms (Gulf Stream), equatorward cools (Humboldt), no sign rule beyond that subtraction — capped ±8 °C, smoothed σ=2 over open water, then walked inland 6 decaying rings (×0.55/ring) so coasts bend while interiors keep their continental truth; the bias joins `tmean` at f64 in `stage_climate` **before** precipitation, biomes, permafrost, treeline and ice read it, computed off the pre-widen coastline by the very `Currents::compute` law the dawn runs post-widen (compute now takes the water mask, one law two coastlines); warm-coast delta +1.01..+1.04 °C over 687–880 rim cells (banded +0.75..+6 sweet), cold-coast −1.16..−1.21 °C over 1508–1577 cells (−6..−0.75), net world bias +0.049..+0.059 °C (≤0.3 — advection redistributes heat, it must not mint it); the shifted isotherms re-rolled every downstream hash and the suite held: cross-runtime deep-earth identity agrees native↔wasm, and the sweep surfaced a real M6.1 breach — the four deposit-anchored M14.8/M5.3 chronicle entries (ore-out, groves-to-stumps, last-timber, green-returns) shipped without ids and the resolve_events fallbacks can't save them (name lookup dies on a same-tick rename; the ground fallback reads the pit, where no town stands): prospecting now resolves the near town to its registry entity by position at emit time (`near_settlement_ent`, renames can't move ground); full suite 577 pass · 14 warn · 0 fail (warn set is the standing forest-amplitude family + perf/pack/wasm rows, three fewer than M40, no new members) |
| M42 | Current-Aware Climate | 2026-08-20 | climate lane ×3 seeds, two new rows green — the rain belts now answer the currents: the precipitation march carries a marine-layer stability memory (`stab`) relaxed toward `1 + STAB_GAIN·heat` over sea (STAB_MIN 0.45 caps the cold-rim inversion — the Atacama lid) and toward a warm-only target over land, scaling land rain-out; warm rims additionally destabilize evaporation (`EVAP_GAIN`), inject moisture on landfall (`WARM_INJECT` 0.020) and feed a direct lateral rain term (`WARM_RAIN` 0.012 — the Gulf-Stream/Kuroshio humid-east-coast import); measurement uses aspect-matched baselines (rim coastal land vs neutral coastal land of the same windward/leeward aspect at its latitude, not the global zonal mean) scoped sub-polar (lat < 60°) since polar cold rims are dominated by spillover drizzle off their own lowered caps; cold-rim suppression 0.40–0.53× ×3 seeds (banded 0.25–0.80 sweet — the Atacama law), warm-rim boost 1.04–1.09× (1.02–2.20 — the Gulf-Stream law), desert 17.5–20.1%, land mean precip 790–815 mm, all sweet; the shifted rain re-rolled every downstream hash and the stale shipped wasm answered with an honest ice-layer DIVERGE (modern glaciers read precip) that vanished on rebuild — the M31 content-hash staleness guard doing exactly its job; full suite 578 pass · 18 warn · 0 fail (warn set is the standing forest-amplitude family + perf/pack/civ-cadence rows, no new members), deep-earth identity agrees native↔wasm |
| M43 | The Tides | 2026-08-20 | terrain lane ×3 seeds, seven new rows + two musts green — tidal range as a derived field of basin geometry (`tides.rs`): every world-ocean coastal cell starts at `TIDE_BASE` 2.0 m, Green's-law shoaling lifts the shallow shelf (`(D_REF/depth)^0.25`, depth floored at 4 m), and a BFS up each confined arm (confinement ≥ 0.55 of the 11×11 water ring, ≥ 8-cell penetration) applies quarter-wave funnel resonance with saturation — never a straight-coast amplifier; landlocked seas (no path to the world ocean) are pinned at `TIDE_ENCLOSED` 0.30 m exactly, the Mediterranean law, and the σ=1.5 smoothing normalizes by the ocean mask (`G(range·w)/G(w)`) so no macrotidal bleeds across an isthmus; landforms gained TIDEFLAT + ESTUARY under a formation law — flats where the intertidal run `range/tan(slope)` spans ≥ 1000 m on ≤ 16 m/cell relief shore, estuaries where a discharging river meets ≥ meso tidal water; measured: open coast 2.89–3.09 m, macrotidal 2.7–7.8% of coast, flats 25–111 cells/seed at 2.08–2.43× the coastal mean range and 0.16–0.19× its relief (both scaling gates), estuaries 4–103; the stamp joins `landform::hash` → hash_state + the deep-earth identity line (agrees native↔wasm), and the M26 regen gate now walks all three stamps (classify + patterned + tidal) — the first suite run caught the regen leg missing the new stamp, exactly the omission that gate exists to read; full suite 585 pass · 20 warn · 0 fail (+2 vs M42: wasm 3.20→3.21 MiB crossed the ≤3.2 lean-budget edge — the honest binary cost of the module, staged below — and a 442 ms long-task flicker on a loaded host) |
| M44 | Longshore Drift | 2026-08-20 | terrain lane ×3 seeds, four new bands + three musts green — the coast moves sand along itself (`coast.rs`): per-pass CERC transport `Q = (w·n)⁺·(w·t)·fetch` off the three zonal wind bands against the σ=2-smoothed shore normal, deposition where the alongshore flux converges (top-8 sites per pass × 12 passes, growth-continuation bonus 1.5 per deposited neighbour so tips elongate into chains); barriers are not born of convergence — a breaker-line nucleation rule seeds ≤5 bars on wide gentle shelves (shallow01 σ=2 density ≥ 0.45, offshore 3–8 cells, ≥6 cells open sea windward, depth above `BAR_FLOOR` −0.02 ≈ 80 m: the bank a 4 km grid actually resolves, Earth's literal 16 m breaker depth is subgrid) and the walk elongates them like any tip; classification is structural — 8-connected deposit chains rooted on the pre-drift shore are SPIT, free-standing are BARRIER, world-ocean water that lost its path out is LAGOON; measured ×3 seeds: share of coastal cells 3.30–3.73% (spec gate 0.5–4), spits 92–97 cells in 22–39 chains, barriers 4–9 cells in 3–4 chains (777 welded 2 of its 5 bars ashore — honest tombolos), lagoons 2–20 cells, ledger↔grown-ground equality and all-shoreface musts hold; the pass runs at the end of `stage_glacial` on the pre-widen f64 height so climate/hydrology read the grown shore, margins widen form+ledger; **the wasm replay caught a real discipline breach**: coast diverged cross-runtime while every other layer agreed — the new `coast_debug` bisection instrument (pos/bits/form split, cf. `seismic_debug`) proved deposit positions and form identical with only the raw f64 pre-height bits differing, i.e. the f64 height field carries runtime-local ULP noise from upstream transcendentals and coast was the first layer ever to hash raw f64 height bits (every agreeing layer hashes f32-quantized values — tides reads the f32 grid outright); the ledger now records the buried seabed at f32 (the engine's portable currency), law documented on the struct: *never hash raw f64 derived from the height field*; deep-earth identity restored native↔wasm (coast=ad39b0a8e75c39b3 both legs, seed 777 × 240 mo), drift ledger replays byte-identical across chunkings, `Coast::hash` joins hash_state + the identity line; full suite 606 pass · 21 warn · 0 fail (warn set is the standing forest-amplitude + perf/pack/wasm-size families plus known cadence flicker, no M44 members) |
| M45 | Harbor-Shelter Scoring | 2026-08-20 | civ lane ×5 seeds — the coast read the way sailors read it (`settlements::shelter_score`): per-coastal-cell enclosure (land share of the r=6 disc, rebased so a straight strand scores 0) × calm (1 − mean 8-ray fetch / `SHELTER_FETCH_CAP` 48 cells = 192 km — measured: at cap 24 basin calm and open-coast chop were nearly alike, 48 spreads them cleanly), plus M44 form bonuses (`SHELTER_LAGOON` 0.35, spit-lee 0.15); solved pre-widen at the dawn on the f32 grid, margins widen it, field joins `hash_state` and `explain.rs` prints the harbour line each port's founding actually priced. Founding takes the **best takeable anchorage** within its 5×5 neighbourhood and prices it convex (`4.5·sh + 4.5·sh³` — Genoa's law: linear shelter lost every auction to soil, the cubic head makes a pocket bay outbid a fertile strand only when the pocket is real); trade prices the same field twice — an embark toll `(1−sh)²·8` on every sea departure, and far-sea lines gated on `SHELTER_ROADSTEAD` 0.5 at *both* ends, so open-strand villages keep fishing while sheltered ports carry the blue-water web. Gate honesty: raw top-quartile concentration punished founders for water already inside a neighbour's spacing ring — the measure is now **well-sited** = on top-quartile water ∨ took the best takeable water within 56 km (excluding claimed cells); measured 87 / 78 / 76 / 90 / 82% (seeds 12345 · 777 · 90210 · 31337 · 555, gate ≥70). Wasm rebuilt (stale-binary check caught by the M31 content-hash stamp), deep-earth identity native↔wasm agrees layer-for-layer (seed 777 × 240 mo); full suite 620 pass · 18 warn · 0 fail (all warns are the standing forest-amplitude + perf/pack/wasm-size families and cadence flicker, no M45 members) |
| M46 | Priced Sea Lanes | 2026-08-20 | economy lane ×3 seeds — open water finally has a grain (`trade.rs`): every blue-water step prices by heading against the M41 gyre field and the zonal wind bands, `mult = 1 − SAIL_CURRENT_GAIN 0.55·(current·ĥ) − SAIL_WIND_GAIN 0.25·(τ·ĥ)`, becalmed convergence rows carry `DOLDRUM_PENALTY` 0.3 both ways; **admissibility is exact, not assumed** — the old global `SAIL_MULT_FLOOR` 0.5 could still price a mixed coast/open block (~0.45 base) under the heuristic's `PLAN_COST` 0.3, so each cell clamps to `FLOOR.max(PLAN_COST / cell_cost)` and the ceiling 2.6 makes dead-upwind gyre limbs the passage nobody sails, which is exactly what A* then routes around; the ledger books the **round trip** — a lane's price is the mean of the out and home passages, so no town gets rich on a one-way current it must beat on the return; the must-gate is identity, not vibes: every found lane re-walks under the same edge law to the same cost, 0 divergent lanes ×3 seeds; the spec's own metric — with-current passage vs its seed-matched against-current mirror, `adv = 1 − out/home` — measured 22.8 / 12.2 / 10.0% best (12345 · 90210 · 777) at 40 mo, band floor set at the spec's 15% and the gate seed clears it; grown in (ECO_YEARS, 74 lanes) the best advantage rises to 29.3% while alive share dilutes to 11% WARN — honest: cabotage hops and harbour tolls are symmetric by design and the open-water fraction of a lane bounds its achievable asymmetry (777's one far-sea hub crosses near-perpendicular to its gyres); `widen` pads cu/cv/open/wind/becalmed so margins never invent current; wasm rebuilt, deep-earth identity native↔wasm agrees layer-for-layer (seed 777 × 240 mo); full suite 619 pass · 21 warn · 0 fail (warn set is the standing forest-amplitude + perf/pack/wasm-size families, cadence flicker, and the M46 alive-share dilution noted above) |
| M47 | Upwelling Zones | 2026-08-20 | climate lane ×5 seeds (12345 · 777 · 90210 · 31337 · 555) — the nutrient coasts (`climate::upwelling`): a 0..1 scalar on ocean cells touching land, `UPWELL_WIND 0.55 · wind_off + UPWELL_COLD 0.45 · cold`, where `wind_off` is the offshore Ekman proxy restricted to **easterly trades only** (westerlies excluded — they drive the warm western-boundary rims that confound the analogue) and `cold` reads M41's heat anomaly on the adjacent water; a smoothstep latitude favor runs full equatorward of 35° and dies by 60°, pinning the marks on the subtropical eastern-boundary systems (Humboldt/Benguela/Canary); solved at the dawn off the final post-widen coastline like territory and the gyres, joins `Fields` + `hash_state`, ships as a u16 wire lane (pack 23.0 B/cell — the bench band re-derived to 23/26 sweet/hard, the honest 2 B/cell cost of the lane), and `explain.rs` prints max nearby upwelling in every port's site provenance — Era IV's fisheries will harvest it; measured: nutrient-rich share (`NUTRIENT_RICH` 0.34) 6.0–7.0% of coastal ocean cells ×5 seeds (banded 3–10 sweet — Earth's eastern-boundary analogues), marked rims −0.09..−0.14 °C against their own row's coastal mean (the west-coast-desert precondition, latitude-controlled), 0 strays (open ocean and land stay exactly zero); the suite's first run caught the properties lane panicking on the unregistered lane (`unknown f32 field upwelling` in the pack round-trip decoder — the wire gate refusing a lane it can't verify), registered and re-run to 42 pass; determinism lane green (regen ×2 identical, 12- vs 60-mo chunking invariant), wasm rebuilt, deep-earth identity native↔wasm agrees layer-for-layer (seed 777 × 240 mo); full suite 626 pass · 22 warn · 0 fail (warn set is the standing forest-amplitude + perf/pack/wasm-size families; the pack-B/cell row now names M47's lane as its cause) |
| M48 | Sea-Route Seasonality | 2026-08-20 | economy lane ×5 seeds (12345 · 777 · 90210 · 31337 · 555) — the sailor's calendar (`trade.rs`): the monsoon lands breathe over their seas — the land's signed rain-lean (`pamp`, M2's monsoon amplitude) smoothed σ=4 along the coast, normalized by the smoothed land mask so coastal amplitude stays the land's own, and walked offshore on a smoothstep window that dies by `MONSOON_REACH` 24 cells (96 km: far blue water answers to the gyres, not the shore); every route books its sea legs' mean lean as a double-frequency throughput season (`season_mult` through the exact COS12 table — both monsoon heights carry cargo at `MONSOON_TRADE_GAIN` 0.55, the two turns of the wind becalm it — diluted by sail share exactly like the barge ramp's law), and gale water (`MONSOON_GALE` 0.40; the field's p90 runs 0.28–0.35, so the gale is the honest top sliver) closes the whole journey for the wet monsoon's three burst months, exactly the pack-ice law; closures are now **labeled** — `SeasonalClosure::{Ice, Monsoon}` on a `shut` vec with `closed` their union, so the ice census reads its own label and a summer burst can never trip the winter-arc law, and any future season (storm coasts, flood closures) joins the enum instead of growing another field; gale cells surcharge A* pro rata at 0.5 (the ice law's gentler sibling — a burst is waited out, never iced in) and `widen` extends the lean like the ice calendar; measured: lane share 24–39% of sea-touching ×5 seeds (banded 15–55 sweet — the dhow-trade share of a tropics-heavy sea), 24–39 lanes shut in the burst per seed, 0 malformed arcs (every burst ⊆ the two hemisphere arcs the law can emit), throughput swing mean 74–92% (band floor the spec's own 30%), the ledger obeys the calendar (0 mismatched route-months over 12 ticked months — flow exactly zero when shut, ice or gale, alive when the water opens), and the month-by-month route state replays byte-identically (`calendar_hash`: FNV over every seasonal lane's 12 shut/mult entries, equal across a full second world); sweep 100y ×5 seeds: calendar lanes 20–39 per seed, all bursts well-formed; wire: `season` + labeled `shut` ride the route JSON skip-if-empty (zero cost off the monsoon seas), genjs re-introspected the contract; M46 note: the gale surcharge moved the gate seed's best mirror advantage 23.0→22.8% (777/90210 unchanged at 10.0/12.2) while the calibration probe surfaced pre-existing thinness on seeds M46 never probed (31337 best 5.9% · 555 7.2% vs the 8 hard floor) — outside the suite's gate seed, staged below; wasm rebuilt, deep-earth identity native↔wasm agrees layer-for-layer (seed 777 × 240 mo); full suite 635 pass · 21 warn · 0 fail (one fewer warn than M47 — the standing forest-amplitude + perf/pack/wasm-size families, no M48 members) |
| M49 | Ocean Diagnostics | 2026-08-21 | ocean lane ×5 seeds (12345 · 777 · 31337 · 90210 · 555) — the ocean stack gets its own instrument panel (`diagnose ocean`, wired into `report.sh` → SUMMARY.txt): gyre topology read as labeled basins ≥2500 cells (1.0 basins/seed, 32k mean cells per gyre, rotation sense 10/10 correct — clockwise north, counterclockwise south), surface-current p95 0.965 with a 3.1× western-boundary intensification (the Stommel signature, banded 1.5–12), current-coast anomalies measured against each row's *own* coastal mean so latitude can never author the result (warm rim +0.63 °C · cold rim −0.81 °C, opposite sides every seed, ≥50 sampled cells per rim per seed so no anomaly is one bay's opinion), M47 upwelling coverage 6.7% of coastal ocean at median |lat| 8°, and the M48 calendar summarized as seasonal-lane share 72% with a 99pp p90−p10 throughput spread (the fleet's year must differ lane to lane, not move as one). 15 pass · 0 warn · 0 fail across the sweep |
| M50 | Metamorphic Ocean Checks | 2026-08-21 | ocean metamorphic lane ×5 seeds (`diagnose ocean --metamorphic` → `ocean-meta.txt`, wired into `report.sh`) — the stack proves it *responds*. A synthetic poleward warm ribbon (`climate::META_WARM_V` 3.0 · `META_STRIP` 16 cells · `META_LAT` 10–70°) is injected on both hemispheres and then killed, and the pipeline is re-solved end to end (current_bias → temperature → precipitation) on the centred square domain the climate march was solved on (ADR-0014's margins cropped, not simulated). **Gate body: the sea rim** — the ribbon water that actually touches shore, i.e. the water the current directly warms — cools 3.84–4.30 °C mean (worst seed 3.84, mean 4.11, floor 2.0 °C), 2.9–3.2k rim cells per seed. The land-coast drop is retained as a *reported secondary* (1.89–2.07 °C, mean 2.02): `HEAT_COAST_DECAY` bounds by construction how much of a rim anomaly can ever cross the shoreline, so gating land temperature would gate the kernel's attenuation constant rather than the ocean's response — the honest measurement is on the body the relation acts on, with the land figure printed beside it. Rain follows the water that fed it: coastal precipitation falls 124–147% mean with 88–92% of cells drier without the current (rule, not average — floor 70% of cells). Control leg: 0 interior cells beyond `HEAT_COAST_RINGS` move by one bit, so the reach is coastal by measurement. Ladder leg: each blue-water lane keeps its own A* path while the water under it is scaled ×0 → ×2 (`META_CURRENT_LADDER`), 4–9 lanes per seed with ≥3 open-water cells; favourable passage never dearer rung to rung and strictly cheaper end to end (1% tolerance where `SAIL_MULT_FLOOR`/`PLAN_COST` admissibility clamps saturate a lane's fastest water), adverse passage strictly rising every rung, and the out-vs-home gap widening strictly (+3.81 cost gained ×0→×2) — the clamp bounds neither of the latter two from below, so the strictness lives where no clamp can fake it. 9 pass · 0 warn · 0 fail ×5 seeds |




## Ready queue

- **The M2.5 spacing row reads every town, its target speaks of settled
  cores (staged 2026-09-05).** `median town spacing` is the median
  nearest-neighbour distance over *all* settlements, banded 14–48 km
  sweet against a target worded "market-town band ~15–30 km in settled
  cores". The M97 closure moved it 34 / 38 → 48 / 53 km on the two civ
  seeds with the town count unchanged (105 / 102): fewer famine deaths
  left the cores +25% fuller on 12345, the crowding term (`0.72·kland`)
  pushed colonies to the frontier, and frontier towns' far neighbours
  lift a world-wide median that the band was never meant to read. Two
  honest restatements, pick one and re-derive the band: (a) measure the
  median over towns with ≥3 neighbours inside 80 km (the settled core
  the target names) and report the frontier's own spacing beside it, or
  (b) keep the world-wide median and reword the target to the whole
  settlement pattern with a band drawn from the sweep. Neither is a
  balance change; the row is WARN inside the hard band today. Touches
  diagnose.rs (civ lane, M2.5 block), settlements.rs (`BANDS`).

- **Ruin cause attribution mislabels famine-hollowed forts as war
  (staged 2026-08-27).** The abandonment sweep's cause heuristic
  (`world.rs`, the `fell_settlement` caller) reads `fort > 0 &&
  pop*3 < peak` as `"war"`, so any fortified town hollowed by the
  M77–M81 mortality stack is chronicled as "war broke it and none
  returned" — the M81 closure's patina lane showed 109 late war-broken
  ruins against 3 actual wars in the politics ledger, which misled the
  ruin-rate diagnosis toward a war mechanism that wasn't there. The
  fix is legibility, not balance: attribute `"war"` only when the
  politics ledger carries a matching sack/siege near the fall, and let
  hunger-hollowed forts read as famine. Touches chronicle text and the
  civ lane's ruin-to-beat matching, so it is its own item, not a
  closure rider.

- **Re-arm the M55 ensemble row as a must (staged 2026-08-27).** The
  row `the veto bites somewhere in the ensemble` was demoted must → want
  at M81 (see the Notes ledger for the full decision record): M81's
  population toll lawfully moved every canonical desert win past
  deep-well craft, so no composed 150-year history holds a pre-craft
  auction and the row has nothing to bite. The row keeps measuring and
  flips back to PASS the moment any composed world carries a refusal
  again; re-promotion to `c.must` is one line in `diagnose.rs`
  (`cmd_gate`). The honest re-arming lever is on the colonisation side —
  an Era II/III phase that gives early extraction a principled reason to
  reach dry ground before deep-well craft (the M56 caravan pressure
  arrived at dawn-adjacent tech; nothing today pulls a *young* people
  desertward) — not on the flood side, where every calibration tested is
  non-monotone trajectory chaos.

- CPU/GPU lens parity probe (discovered building M63): atlas.rs now
  generates the palettes and WGSL ramps both paths consume, but the
  *blend rules* — MDOW hillshade weighting, undersea province dimming,
  categorical alpha — are still hand-mirrored between hillshade.wgsl
  and render/compositor.js. A drift there ships silently: the wgpu
  path renders one map, the CPU fallback another, and no gate notices.
  Extend browser-probe.py to render every lens on both paths at a
  fixed camera and compare downsampled pixel signatures within a
  stated tolerance band. Touches scripts/browser-probe.py,
  render/compositor.js, gpu.js (forced-fallback flag).
- Feature naming reads the landform lane (discovered building M62):
  the M3 feature detector still finds capes, deltas and marshes by its
  own morphology heuristics, parallel to — and occasionally in
  disagreement with — the M60 landform vocabulary the cells already
  carry. Seed feature detection from the landform lane instead, so a
  feature called "The Mouths of the Karia" stands on cells the map
  itself calls delta. Touches naming.rs (add_features), landform.rs.
- Provenance chain names its actors (discovered building M61): the
  "why is this here" chain currently speaks of anonymous forces — "the
  river", "the ice", "the sheet". The naming registry already knows
  which river, which range, which proglacial lake cut which spillway;
  let chain rows cite the named feature at or upstream of the cell so
  the card reads like a story ("the Karia laid this silt", "the ice of
  the Veldmark trough ground this floor deeper"). Touches explain.rs,
  naming.rs (feature lookup by cell), web inspector (row links that
  select the feature).
- Karst Country II (displaced from the original M56 slot when the
  operator re-scoped M56–M58 onto the caravan frontier): limestone
  country behaves like limestone country — a karst pass on M18's
  carbonate provinces generating Poisson-disk-seeded sinkhole clusters
  scaled by precipitation, disappearing-river segments that re-emerge
  downstream as springs (mass-balanced within 2% across the sweep),
  and cave tags for later chronicle/ruin hooks. M20 landed the first
  karst pass; this is its deepening. Touches geo.rs, hydrology.rs,
  biomes.rs.
- The coast law's third executor answers a gate (discovered building
  M67): the JS port of the integer JFA in `render/compositor.js` is the
  one executor no harness holds — the WGSL kernel and the Rust twin
  prove byte-parity on lavapipe every suite run, but the wasm-less
  browser path is a hand-mirrored copy, the exact fork shape
  ADR-0026/0027 exist to kill. Export the fixture's golden seed field
  from the Rust twin and run the JS port against it under bun as its
  own suite lane (byte-equal or FAIL); real-world spot checks fold into
  the standing lens-parity item's probe. Touches report.sh (new lane),
  scripts (golden export), render/compositor.js.
- River Forms II (displaced from the original M58 slot): meanders,
  oxbows, terraces and braids from valley slope and sediment load — a
  Howard–Knutson sinuosity model (migration velocity ∝ curvature) on
  low-slope reaches for meander belts and cutoff oxbows, terrace
  classification for valley-floor steps, braided flag where load
  exceeds capacity; gated on sinuosity rising monotonically across
  slope deciles and oxbow/braid counts in bands. M59's sediment budget
  has landed — this item is now unblocked and can read the real
  per-cell load the ledger books.
- Sabkha/playa vocabulary (staged by M60): the flat desert floors
  where the M54 water table daylights and clamps — measured at 5–11k
  cells/world — currently tie out of the strict-minimum oasis law and
  read as generic plain. They are evaporite country: a SABKHA landform
  word seated in the physical condition (arid ∧ daylighted table ∧
  flat), plus salt/gypsum deposit hooks for the economy and a bright
  playa albedo in the satellite pass. Gate: every sabkha cell arid and
  daylighted by construction; share of arid land in a measured band;
  the oasis pointlike band must not move.
- The warm rim's rain (M42/M59 residual): the Gulf-Stream rain boost
  reads 1.00×/0.98× against a 1.02 sweet floor even after the
  delta-flat exclusion made the sample relief-honest. The residual is
  mechanism, not sampling: the advection march reads the heat anomaly
  into evaporation but the warm-rim column's *capacity* term doesn't
  rise with it, so warmed onshore flow carries barely more water than
  neutral flow. Re-derive the moisture-capacity coupling from
  Clausius–Clapeyron (≈7%/°C, the one law that sets it) and let the
  band be re-measured — no threshold moves until the physics does.
- Sealing Era I — closed by the M55 gate re-derivation, not by a softened band. The held row
  was a measurement error, twice over: the counterfactual judged each
  dry town against its people's *terminal* well craft rather than the
  craft they held at the founding instant the veto actually runs, and
  it demanded that every world's 150 years exercise a law that lives
  in the model, not in every history. Corrected on both counts, seed
  12345 shows 2 refused towns (the founding-time clock surfaced one
  the terminal framing had lost), and seed 777's zero is a true
  history — all three of its dry foundings fall after month 1080, with
  aqueduct/engineering reach (60–90 m) over tables 30–35 m deep, so the
  veto ran and had nothing to refuse. The per-seed row is a WARN
  carrying its full evidence; the law itself is enforced at ensemble
  scope. The gate now prints SEALED on its own, as promised.

- The wasm size diet, residual (E6.4): the gen monolith half of the
  old diet item closed with M66 (stage boundaries survive binaryen;
  heaviest own item now `stage_dawn` 3.44%, domination row PASS, and
  the split *shrank* the shipped binary ~6 KB) — but the binary still
  reads 3.35 MiB against the 3.0 sweet band, grown 2.86 → 3.35 across
  M41–M65. The audit's table names the real candidates: the naga
  WGSL-validate + GLSL-writer chain (~300+ KB across rows) runs full
  shader validation at boot for shaders that never change — a
  pre-validated pipeline (validate in the harness, ship the module
  trusted, keep the validator behind a debug feature) is the one cut
  whose size is already measured. Any cut proves `hash_state` and the
  deep-earth identity line unchanged — size may never buy drift.
- The dawn stage as the next legibility target: with the monolith
  split, `stage_dawn` is the heaviest own body (134 KB — founding,
  cultures, goods, routes and the hashed dawn ledgers in one stage).
  If a future audit run shows it growing, the same `#[inline(never)]`
  + `--no-inline` recipe applies one level down (its inner passes are
  single-caller too); today it is within band and left alone.
- The fertility kernel's residual cost (E10.1): 152–186 ms against an
  80 ms sweet band even after the SOR face-transmissivity and
  flat-slice work — the remainder sits in the M54 Darcy solve's fixed
  sweep counts (90/34/12 at stride 4/2/1) and M51's soil genesis.
  Candidate cuts that keep determinism: derive sweep counts from a
  measured convergence profile per stride (fixed numbers, re-derived
  honestly), initialise the fine grid from the coarse solution with a
  better prolongation, or fuse the soil-order curve lookups into one
  table pass. No band may move.


- Upwelling on the client (M47 follow-on): the lane ships on the wire
  already but nothing draws it — the satellite layer could tint
  nutrient-rich water the cold green-grey of a real eastern-boundary
  system (against the warm-basin blue), and the inspector could grade
  any coastal cell's upwelling index the way it names tidal class.
  Era IV's fisheries phases will want the visual anyway; the render
  cost is one palette read off a lane the client already decodes.

- The sailing calendar on the client (M48 follow-on): `season` and the
  labeled `shut` arcs ride the route JSON already (types.js carries the
  contract) but nothing draws them — the inspector could print a lane's
  twelve-month calendar strip (■ shut · the double-frequency swell in
  between) and the sea-lane overlay could fade or gap the dash during
  the current month's closures, reading the same wire fields the ledger
  obeys. Pairs naturally with the M46 directed-dash item below.
- Seasonal edge pricing (M46/M48 residual): M48 landed the monsoon as
  closures + throughput on the annual-mean-priced web; the deeper cut
  re-prices the *edge law itself* by month from the two solstice wind
  fields, so A* sails out on one season's wind and home on the other
  and route geometry itself becomes seasonal. Instrument first:
  per-lane open-water fraction (measured to bound achievable asymmetry —
  cabotage hops and harbour tolls are symmetric by design, so the
  alive-share band reads cleaner conditioned on it), then the seasonal
  gains re-measure the mirror-advantage bands from the solstice pair.
- M46 pricing on the unprobed seeds (M48 finding): the mirror-advantage
  band was calibrated on 3 seeds and the M48 probe measured all 5 —
  31337 runs best 5.9% and 555 runs 7.2% against the 8 hard floor,
  pre-existing (777/90210 byte-close to their M46 values) and outside
  the suite's gate seed. Either the M46 gains re-derive from the
  5-seed envelope or basin geometry honestly bounds those two seeds;
  the open-water-fraction instrument above decides which story the
  band should tell.
- Directed lanes on the client (M46 follow-on): out and home now price
  apart engine-side but the map draws one undirected dash — the sea-lane
  overlay could weight or animate the dash along the cheap heading and
  the inspector print both passages (`out 41.2 · home 53.7 · rides the
  western gyre`), reading straight off the route ledger already on the
  wire.
- Wasm binary diet (E6 follow-on): the shipped binary crossed the
  ≤3.2 MiB lean-budget sweet edge at M43 (3.21 MiB; the older ≤3.0
  E6.4 band has warned since ~M41). Before the next few phases push it
  toward the 3.4/4.0 hard caps, run a `twiggy`/`cargo-bloat` audit and
  reclaim: likely candidates are monomorphized ndarray zip chains and
  serde derives on tick-lane types that could share shapes. Budget
  bands re-derive from the measured post-diet size.
- Client rendering of the tidal shore (M43 follow-on): TIDEFLAT and
  ESTUARY ship in the landform grid already; the renderer could draw
  flats as a sand-sheen band at the waterline and the inspector could
  print the local tidal range (meso/macro class) per coastal cell —
  the range field itself would need a pack lane or a header table.
- CoastForm on the wire (M44 follow-on): spits, barriers and lagoons
  live engine-side only — the flags byte is full (8 bits), so shipping
  them means either a landform-stamp route (SPIT/BARRIER/LAGOON join
  the landform vocabulary like TIDEFLAT did) or a second flags lane.
  Client: sand-toned deposit cells distinct from bedrock coast, lagoon
  water tinted still and shallow, and the inspector naming the form —
  M45 harbor-shelter scoring reads the same field engine-side already.
- Shelter overlay on the client (M45 follow-on): the anchorage field
  lives in `hash_state` and each port's `explain` ledger prints its
  harbour line, but the map itself can't show *why* the coast is good —
  a per-cell shelter lane (u8 quantized, coastal cells only, RLE like
  territory) would let the satellite layer tint calm water and the
  inspector grade any shore a founder never took.
- The missed cohort (M45 finding): 7–14 ports per seed still sit
  within 56 km of strictly better takeable water — the founding scan
  is ±2 cells and widening it flattened the embark-toll price signal
  (measured, reverted). A post-founding harbor walk — one coastal
  relocation step toward the best takeable anchorage within a day's
  sail, before the name is given — would close the residual without
  touching the auction; gate re-measures well-sitedness with the walk
  on and the toll discrimination bands unchanged.
- Hash-discipline audit (M44 finding): the f64 height field carries
  runtime-local ULP noise from upstream transcendentals, proven by the
  coast bisection split (positions agree, raw f64 bits diverge
  native↔wasm). Coast now records f32, but nothing *prevents* the next
  layer from hashing raw f64 height derivatives — an assay property
  that walks every `hash_state` contributor and rejects raw f64 bits
  sourced from height-lineage fields would turn the convention into a
  gate.
- Holocene lake attrition (M39 follow-on): proglacial-giant density
  runs 6.8–21.7 per Mkm² formerly iced vs Earth's ≈1.5–4.5 — every
  moraine-dammed basin the retreat left still holds water, because
  nothing breaches a dam or fills a shallow after the ice goes. A
  patina-stage attrition pass (spillway incision breaches a share of
  the dams; peat/sediment infill claims the shallowest basins, leaving
  fertile drained-lake flats the way the old lake plains feed farms)
  would bring the density onto Earth's fringe and hand agriculture a
  real landform instead of deleting lakes by fiat.

- Seasonal-swing dispersion (E5 recalibration): land amplitude runs
  mean 5.8 °C · p90 9.4 vs Earth's ~12 · ~25 — the whole world reads
  maritime. M38's GDD treeline faithfully reports the consequence: the
  boreal belt starves (0.5% of land vs 2.5% under the annual-mean law)
  and forest share sits in WARN (20–24% vs sweet ≥25). Widening the
  continental swing reopens calibrated ledgers — M33 permafrost extent
  + frontier asymmetry, M37 ice calendars, M38 offset bands, sweep
  forest/desert bands — so it moves as its own phase-sized item, all
  bands re-derived together, not as a treeline-side fudge.
- Client rendering of icebound lanes: `Route.closed` ships in meta;
  overlays.js could draw a route pale/dashed during its shut months
  and the inspector could print the ice calendar per lane.
- Client rendering of braided reaches: the BRAIDED bit ships in the
  pack flags already; the renderer could draw braided segments as
  split gravel-bar channels (overlays.js / gpu shader river pass).
- Loess source coupling: `loess_mantle` still deflates off the till
  margins; physically the outwash plains (M32) are the primary
  deflation source — recouple once M33 lands so the two calibrations
  move together.
- Client rendering of the frozen rim: PERMAFROST/PATTERNED bits ship
  in the flags byte; the satellite layer could stipple polygon nets
  and stripe texture on patterned cells (compositor.js), matching the
  inspector note that already names them.
- Client rendering of modern glaciers: the GLACIER bit (128) ships in
  the flags byte; the satellite pass could paint true glacier white
  (crevasse-blue tint) distinct from seasonal snow, which currently
  reads only temperature (gpu shader / render.js).
- Client rendering of ocean currents (M40 follow-on): the ψ/u/v grids
  live engine-side only — an overlay of current streamlines (or sparse
  arrows scaled by speed) on the satellite layer would make the gyres
  legible, and the inspector could name the current regime per ocean
  cell (western boundary · interior drift · doldrum) from the same
  fields M41 will read for heat transport.
- Size-aware oatmeal bands for the landform mix (discovered building
  M64): the era lane's landform oatmeal floor reads 0.0015 at the hard
  edge — but the era fold runs 16 variants at 256², where the landform
  vocabulary compresses (short coasts, little ice, few big basins) and
  the closest pair legitimately converges. The floor was derived on
  richer ground than the lane measures. Either re-derive the band at
  the size the lane actually runs, or run the landform-mix leg at 512
  on fewer variants so the law and the measurement share a world size.
  Touches diagnose.rs (era lane bands).

| M68 | New Grids into the Registry | 2026-08-22 | The era's last two hand-wired grids come home. `coastform` (M44's drift classes) and `silt` (M59's deposition depth) were owned by their ledgers and never crossed the wire; they now live in `Fields` as declared registry rows (`coastform` raw u8, `silt` u16sqrt for its wide, mostly-zero range), and the ledgers keep only what is theirs — the deposit record, the mouth books, the delta mask. The handover happens once, post-widen at the dawn, by move not copy: `Coast::form` → `form_gen` and `Sediment::depth` → `depth_gen` are renamed so any stale post-dawn reader fails to compile rather than reading an emptied array; `Coast::hash` and `Sediment::hash` now take the live grid, so replay identity covers the same bytes it always did. Both rows are `hash false` in the registry precisely because their ledgers already hash them — one grid, one hash, no churn in the sealed identity line. The gate's own hand-mirror went too: the pack round-trip lane's name→grid maps in `diagnose properties` are replaced by a lookup through `field_decls()`, so a grid registered in `pack.rs` is a grid the gate judges and there is no second list to drift. Measured ×3 seeds at 256: 19 sections, offsets contiguous, CRC32 stamped, raw sections bit-equal, quantization worst 0.500·scale (exactly the half-step bound). Also this pass: the coast law's third executor closed — the Rust twin exports a golden seed field each suite run and `scripts/coast-js-parity.mjs` replays it through the JS port under bun, byte-parity on fixture and world. Setting that gate up caught a real flaw in M67's own lane: it measured `size × size` when the shipped grid is `width × size` (320×256, 640×512 at standard size) — a truncated window of the world the engine actually uploads. Re-measured on the true grid: max |jfa−exact| 0.877 cells, ≤0.001% of sea cells off, still inside band. |
| M69 | The Upload Order Is the Registry's | 2026-08-22 | The last hand-mirror on the GPU path is gone. `set_world`'s positional wasm-bindgen signature cannot be generated from the field table, so the table judges it instead: `pack::gpu_order_is()` is a `const fn` that walks `FIELD_SPECS` and answers whether a name list is exactly the `gpu true` rows in pack order, and `render::SET_WORLD_GRIDS` — the signature written out as data — is checked against it in a `const` assertion. A grid whose `gpu` flag moves without its parameter is now a build break in both the native and wasm legs, where before it surfaced only in the browser as an arity mismatch against the generated `FIELDS` table. The harness reports what it judged rather than trusting it: the atlas lane prints the order and gates on it. Measured, one uninterrupted full suite: `gpu upload order (11): height · tmean · tamp · precip · discharge · fertility · strahler · flags · rock · soil · landform`, PASS. Suite totals 889 pass · 31 warn · 4 fail. The four fails are not this milestone's: `pack bytes per cell` 26.0 B/cell sits exactly on the hard edge since M60's landform byte lane (staged as a pack-diet Ready item, not re-banded), civ-777's M55 dry-frontier veto row remains the honestly held failure it has been, and the two composed rows (`the suite composes clean`, `era I seals`) are the M65 gate reporting those upstream — the era stays held open, by design. |
| M70 | The Measured-Width Bit Lane | 2026-08-22 | The pack diet lands as measurement, not as a re-banding. Pack v3 (ADR-0028) adds a fourth wire lane, `wire bits`: a categorical `u8` grid is written at `ceil(log2(live_max+1))` bits per cell (1..=8), LSB-first, with the width **measured from the grid's own maximum** and recorded on the array entry — no vocabulary size is hard-coded anywhere, exactly the way quantization already measures `scale`/`offset`, so a vocabulary that grows past a power of two widens itself and a new grid costs the bits it earns. Seven grids ride it (`biomes`, `crops`, `strahler`, `rock`, `soil`, `landform`, `coastform`); `flags` stays a full byte because it is a bitfield, not an id. The client expands bit sections to full `Uint8Array` at the `unpack()` edge, so nothing downstream of the wire changed; `validate_pack` sizes a section as the ceiling of its bit run and refuses any width outside 1..=8; `diagnose properties` decodes the lane the same way the client does and demands **bit-equality, not tolerance**. Storage, the GPU upload path and `hash_state` are untouched — this is strictly a wire concern (ADR-0016). Protocol bumped to `PACK_VERSION = 3` and refused by any other client, in Rust, JS and the harness alike. Measured at 512 ×3 seeds (12345 · 777 · 90210): **26.0 → 21.90 B/cell**, blob 8.36 → 7.17 MB — off the hard edge and back inside the sweet band with real headroom for the next vocabulary. One uninterrupted full suite, generated 2026-08-22 09:25 UTC: **877 pass · 33 warn · 4 fail**. Pack v3 holds: no FAIL and no WARN belongs to this milestone, and `pack bytes per cell` PASSes for the first time since M60. The four fails are the standing, independent set, unmasked and unchanged: civ-777's M55 dry-frontier veto row (0 towns on ground the veto refused — the honestly held failure it has been), the M65 composed rows `the suite composes clean` and `era I seals` reporting it upward (the era stays held open, by design), and `the wasm audit speaks for shipped bytes` STALE AUDIT, bookkeeping owed to the pack-v3 rebuild. That last one is now closed in the same pass: `scripts/wasm-audit.sh` re-run against the shipped binary (subject `calliope_bg.wasm` 3515909 B · sha256 32f8337d…c271bc86, byte-identical to `web/js/wasm/calliope_bg.wasm`), and the gate re-composed to **7 pass · 0 warn · 2 fail** — the audit row PASSes `same bytes`, the heaviest own item is still `GenBuilder::stage_dawn` at 3.45% (staged size-diet Ready item, unchanged). Suite totals after the audit row flips: **878 pass · 33 warn · 3 fail**; the remaining three are civ-777's dry-frontier veto and the two M65 composed rows reporting it — the era stays held open on exactly that one honest failure. |
| M71 | The Year Stops Repeating | 2026-08-22 | Era II opens: every year now draws its own sky. `climate::year_anomaly` builds a per-year, per-cell anomaly pair — `dt` in °C on `tmean`, `dp` as a fractional multiplier on `precip` — from a seeded `Perlin3` lattice keyed on `(x, y, year)`, with amplitude shaped by `lat_deg` so the polar belt swings wider than the tropics (declared 1.5 °C tropical, 4 °C polar, the digest's latitude-shaped variance). The field is **derived, never stored**: `World` carries the `variability: Perlin3` source and a `Mutex`-wrapped one-slot `year_weather` memo, exposed through `with_year_weather` / `year_tmean` / `year_precip`, so a year costs one solve however many readers ask for it, and nothing new rides the wire or `hash_state` (the seed and the year already determine it — ADR-0003). One real finding: `ANOM_FBM_SIGMA`, the lattice normalizer, was a guessed constant and the new “the measured swing is the declared swing” row caught it **20% off**; it is now set from measurement at **0.2076**, and that row exists precisely so the constant can never silently drift again. Measured ×3 seeds over 60 years, land cells only — σ T rises monotonically poleward **0.376 → 0.742 → 1.243** (12345), **0.368 → 0.719 → 1.213** (777), **0.371 → 0.725 → 1.204** (90210) °C; the rain lane follows the same law in fractional terms (0.094–0.097 → 0.127–0.131 → 0.174–0.179); realized amplitude sits within **0.8% / 2.6% / 4.0%** of the declared; zero non-finite cells; the worst year takes −0.66/−0.75/−0.76 of the rain share and never all of it (total failure of the rains stays famine's verdict, M2.6); and one seed-year redraws **bit-identical both fresh and memoized**. One uninterrupted full suite, generated 2026-08-22 16:01 UTC: **898 pass · 34 warn · 0 fail** — the era gate composes clean over all 37 lanes with its 300-year structural leg (seed 12345, 3600 months, chunkings 240/120 vs 180 in agreement), Era I still sealed with the M55 veto row green at ensemble scope. No FAIL anywhere; the 34 warns are the standing bands (forest share, Hack ratio, wasm size, tick rate, oasis share), none of them this milestone's. The anomaly is deliberately **not yet coupled** into harvests, discharge or pasture — that is M72's claim, and it is not made here. |
| M72 | The Weather Reaches the Ground | 2026-08-23 | M71's sky stops being a display layer and becomes causal. `agriculture::year_yield` re-scores the crop curves at the year's *realized* rain and heat instead of the climatology, `hydrology`'s discharge read gains `year_flow_factor` (catchment-mean rain anomaly × GAIN 1.50, clamped to [0.35, 2.20]), and `famine` drops its private die entirely: hunger is now the year's own SPI at the town's cell. The irrigated branch reads the *published* flow law — `year_site_flow_factor` applies the same gain **and the same clamp** as `year_flow_factor`, so a canal can never be fed more water than the river is stated to carry. Performance was the milestone's real cost and was root-caused, not waived: materializing a full 512² anomaly grid every month collapsed year-0 throughput to 104–177 mo/s against a 200 mo/s floor; single-point `year_anomaly_at` / `catchment_anomaly_at` evaluators plus a settlement-scoped `year_site_weather` BTreeMap memo restored it to **1034 mo/s worst-of-3-seeds** (sweet ≥1000) with no gate relaxed and determinism untouched (generation · simulation · 12-vs-60-month chunking all identical, 3 pass · 0 warn · 0 fail). Gates, all measured this run: every famine stands in a failed year (85/85 and 91/91 at SPI ≤ −1.0); no town starves in a year that was not dry (0 of 486 and 0 of 508 wet-side town-years); the toll is the shortfall's own arithmetic (85/85 and 91/91 famines reproduced exactly from the year's SPI, no residual); hunger climbs with the drought (toll 0.0495 + 0.1597·s, ρ 0.9997, quartiles 0.062 < 0.087 < 0.117 < 0.174 — the threshold decides *whether*, the shortfall decides *how hard*); harvest variance tracks the sky (σ 0.2229 vs independent curve re-scoring 0.2229, +0.00%; ρ 1.0000 against the exact re-score, where the old tangent-line model read 0.692–0.960 — the gate was re-derived against the true mechanism, not loosened); flow variance tracks the catchment (σ 0.1410 vs 1.50× catchment σ, ±0.0%); the year is bounded, not deleted (889 clamped harvests, 0 flows out of band); and the river map does not move (dawn and year-150 hashes identical — the year is a read, never a write). Full suite, warm target, 2026-08-23 07:27 UTC: **926 pass · 31 warn · 0 fail**, browser probe and composed Era gate both run to completion; `gate.txt` seals 10 pass · 0 warn · 0 fail over 38 composed lanes at 0.4h freshness, era I **SEALED**. Held open and kept separate: M45's seed-777 300-year port-siting drift, diagnosed independently of this milestone. |
| M68–M72 | Re-banked over one clean suite | 2026-08-25 | The Era-II block M68–M72 is re-verified from a single uninterrupted `scripts/report.sh` run (generated 2026-08-25 00:01 UTC): **928 pass · 39 warn · 0 fail**, era gate **10 pass · 0 warn · 0 fail**, Era I **SEALED**, 38 lanes composed with oldest report 1.0h. The one blocker that held the block open — the year-100 tick rate sitting under the 100 mo/s hard floor — was root-caused to `settlements::colony_site` scanning the whole 640×512 grid per colonization attempt and then discarding everything outside the parent ring. The fix hoists the ring bound into the loop range (`parent ± ceil(sqrt(max_d2))`, clamped to grid) while leaving the exact `d2p` test in the cell body, so the box is a strict superset of the ring and the accepted candidate set and row-major choice order are unchanged by construction. Proved, not asserted: against the pre-change binary, state · events · **town-ledger** hashes are bit-identical for seeds 12345/777/90210 at 1200 months and for seed 12345 at 3600 months, under ×12, ×12-again and ×60 chunkings. `colonize` fell 1878 → 1028 ms per 100y; year-100 tick rate 87–96 → **117–129 mo/s** worst-of-3 against the **unchanged** ≥100 floor (band untouched). Structural leg in-run: seed 12345, 3600 months, chunkings 240/120 vs 180 in agreement, towns 16→96, 707 events in the final decade. The 39 warns are the standing bands (generation time, tick rate, wasm size, event rate, worst long task); no FAIL anywhere. |
| M73 | Variability Held in Bands | 2026-08-25 | Era II's sky gets its instrument. A new `diagnose climate-variance` lane measures the realized interannual σ of both anomaly lanes — heat in °C, rain as a fraction — over land only, across sixty years and the full five-seed sweep, split into the three latitude belts the declared amplitude law separates. Nothing in the gate is a hard-coded magnitude: each belt's realized σ is held against the **belt-mean of `climate::anomaly_amp_t`/`_p` computed over the very cells measured**, so changing an amplitude constant moves target and measurement together and only a genuine normalizer drift can open a gap. Three independent kinds of evidence close it: the declared law (σT within 10%, σP within 12% — the −0.85 rain floor clips its low tail), Budyko–Sellers shape (polar amplification >2× and within 10% of the ratio the law itself predicts for these belts), and the spec's absolute ceilings (tropical σT < 1.5 °C, polar < 4 °C, on every seed). Cross-seed spread finishes the argument: a belt's swing is a property of the sky's law, not of the seed that drew it, so the spread of each belt's σ across the sweep must sit inside a tenth of its mean. Measured, 512 · 60 y · seeds 12345·777·31337·90210·555: σT **0.371–0.377 / 0.719–0.742 / 1.204–1.247 °C** by belt, σP **0.094–0.096 / 0.127–0.133 / 0.174–0.179**; worst declared-vs-realized gap **−4.0% (T) / −4.6% (P)**; amplification **3.25–3.31×** against a declared 3.31–3.32; cross-seed spread **2.5–4.6%**; zero non-finite cells; worst year takes −0.757 of the rain and never all of it. Lane verdict **74 pass · 0 warn · 0 fail**. The year's sky is derived, never stored (ADR-0003), so the identity line carries a probe of its source rather than a grid: `World::variability()` is exposed and a fixed 3-year × 9-cell × 2-lane read of the lattice folds into `hash_state` as the `W` lane. The fold is visible and inert exactly where it should be — the state hash moves `45b0143cae16f01c → ca4b20ac13064303` while `events 0639b0e527eda60c` and `towns a65e823fdcf9abd6` are byte-unchanged, i.e. the probe rides the identity line and the simulation itself is untouched; determinism stays 3 pass · 0 warn · 0 fail. One uninterrupted full suite, generated 2026-08-25 00:42 UTC: **1002 pass · 39 warn · 0 fail**, era gate **10 pass · 0 warn · 0 fail** over **39** composed lanes (oldest report 1.7h), 300-year structural leg identical under chunkings 240/120 vs 180, towns 16→96, Era I still **SEALED**. Tick rate year-100 115–127 mo/s against the unchanged ≥100 hard floor. No FAIL anywhere; the 39 warns are the standing bands. |
| M74 | The Seesaw Seas | 2026-08-25 | The sky gains its slow lean. `oscillation.rs` draws an ENSO-class basin once from the seed — a period in 24–84 months (two to seven years), an amplitude in 0.55–1.45 σ, a starting phase — and mixes a unit-variance sinusoid with a coloured fbm lane at fixed variance shares, so the realized σ *is* the drawn amplitude however the shares are tuned, with a hard 3σ stop so no tail draw hands the causal path an absurd year. The index is a pure function of `(seed, month)`; nothing is stored in the grids, so per ADR-0003 the identity line carries a probe of the source (`Oscillation::probe`, the drawn law plus the index at six spaced months) as the `O` lane beside M73's `W`. The new `diagnose oscillation` lane refuses to read the answer off the struct: it recovers the period **blind**, by scanning a periodogram of the realized index over 1200 months at 0.25-month resolution, and holds the recovered peak against the drawn period. Measured, seeds 12345·777·31337·90210·555: drawn period **27.3 / 67.9 / 75.6 / 29.2 / 28.8 mo**, recovered **27.2 / 67.8 / 75.5 / 29.2 / 28.8** — every seed inside **±0.3%** of drawn; realized σ **1.033 / 0.548 / 1.183 / 1.474 / 0.604** against amplitudes 1.04 / 0.56 / 1.16 / 1.36 / 0.56; mean lean **+0.106 / +0.009 / −0.013 / −0.093 / +0.026**, i.e. every basin returns; warm and cold phases both lived in (20–30% of months each side); the 3σ cap never touched from outside; cycle-length σ **1.9–5.2 mo**, so the seesaw skips and stalls rather than ticking; each seed draws its own rhythm (27.3–75.6 mo across the sweep) and every basin replays byte-identically from the seed alone. Lane verdict **51 pass · 0 warn · 0 fail**. One real defect was found and root-caused rather than gated around: realized σ came in 10–13% under the drawn amplitude on three seeds because the noise normalizer was a guess. Measured over 200 000 consecutive months, the fbm lane's σ is **0.1916**, not the assumed 0.36; the constant was corrected and the 10% gate left exactly where it was. Determinism holds with the fold in place — `3 pass · 0 warn · 0 fail`, generation `bc73f8e50096c0be` twice over, run identical under 12- and 60-month chunkings (`state 426de7ad42d4b3b5 · events 0639b0e527eda60c · towns a65e823fdcf9abd6`), the event and town ledgers byte-unchanged from M73's, i.e. the probe rides the identity line and the simulation itself is untouched. One uninterrupted full suite, generated 2026-08-25 01:22 UTC: **1053 pass · 39 warn · 0 fail**, era gate **10 pass · 0 warn · 0 fail** over **40** composed lanes, 300-year structural leg identical under two chunkings, towns 16→96, 707 events in the final decade, Era I still **SEALED**, year-100 tick rate 109–129 mo/s against the unchanged ≥100 hard floor. |

| M75 | The Tilted Belts | 2026-08-25 | The seesaw reaches across the equator. `climate::teleconnection_bias` adds a hemisphere-antisymmetric tilt to the fractional rain anomaly after the ITCZ gaussian — a Gaussian trade belt centred at ±15° with σ 12°, gain `TELE_GAIN` 0.16, driven by the M74 index on a `TELE_LAG_MONTHS` = 4 lag (`World::year_osc`), so a warm phase wets one trade belt exactly as much as it dries the other. New `diagnose teleconnection` lane, 512 · 120y, seeds 12345·777·31337·90210·555: cross-hemisphere trade-belt rain correlation **r = −0.987 / −0.953 / −0.989 / −0.994 / −0.964** (gate |r| > 0.3 and negative — a teleconnection tilts, it does not lift); the **zero-index counterfactual** falls silent at **+0.024 / −0.021 / −0.033 / +0.100 / −0.042**, so the tie is the oscillation's and not the map's; the belt difference **flips sign with phase** (warm +0.272 / cold −0.217 on 12345, +0.358 / −0.339 on 90210) with both phases lived in (42–59 years each side) and a swing of 0.29–0.70 of the rain, felt rather than cosmetic; the tilt is **antisymmetric to 1e−12**, exactly 0 at the equator, 0.0012 at lat 55° (the westerlies keep the unforced sky M73 measured); and the coupling **derives exactly** — `|forced − counterfactual − bias| ≤ 1.1e−16` on every seed, so no second mechanism hides in the residual. No poison enters the rain (0 non-finite, worst −0.749, the floor never breached) and every tilt replays identically. Lane verdict **44 pass · 0 warn · 0 fail**. One real consequence was handled by re-deriving a mismeasurement, not by moving a threshold: the tilt is a *second, forced* variance source in the tropics, and M71's "rain swings wider toward the poles" row was reading the forced total and attributing it to the unforced latitude law, so on seeds 12345 and 90210 the tropics overtook the mid-latitudes. `World::year_anomaly_unforced` exposes the osc≡0 twin, the M71 row now measures the field its law actually declares — unforced ordering tight and seed-stable at **σ 0.097 → 0.131 → 0.179** — and two *new* checks were added rather than any loosened: the tilt must widen the tropics it aims at (0.097 unforced → 0.157 forced) and must leave the polar belt within 2% (0.179 → 0.179). M73's declared σP is likewise recomposed as `sqrt(amp_p² + tilt_shape²·σ_index²)` from the world's own measured index σ, tolerances unchanged at 10%/12%; `climate-variance` reports **74 pass · 0 warn · 0 fail**. Determinism holds: generation `bc73f8e50096c0be` twice over, run identical under 12- and 60-month chunkings (`state 426de7ad42d4b3b5 · events 0639b0e527eda60c · towns a65e823fdcf9abd6`), 300-year structural leg identical under 240/120 vs 180 chunkings. Wasm rebuilt for the changed rain path (stamp `8856c76f9b09`, 3 528 450 B) and the audit re-run against the shipped bytes — the gate's own row reads **same bytes**; cross-runtime deep-earth replay agrees native vs wasm. One uninterrupted full suite, generated 2026-08-25 03:27 UTC: **1109 pass · 33 warn · 0 fail**, era gate composed last over **41** lanes at **10 pass · 0 warn · 0 fail**, freshness oldest **0.4h**, browser probe live (7/0/0), towns 16→96, 683 events in the final decade, Era I still **SEALED**. |
| M76 | Reading the Seesaw | 2026-08-25 | The seesaw is read back out of the world it forces, blind. Three new spectral gates in `diagnose oscillation` refuse to take the mode's existence on the struct's word: **prominence** (dominant periodogram power against the median off-peak floor, the peak's own ±25% skirt excluded, gate ≥3×) measured **194.6 / 251.3 / 173.6 / 107.9 / 82.8×** on seeds 12345·777·31337·90210·555 — the weakest world clears the bar 27-fold; **single-mode margin** (strongest rival power outside the skirt, gate <50% of peak) measured **5 / 5 / 4 / 3 / 6%**, so what the spectrum finds is one seesaw and not a pair; and **phase coherence** (share of index variance carried by the recovered tone, gate ≥0.30) measured **0.68 / 0.68 / 0.71 / 0.70 / 0.60** — a quasi-periodic mode that holds phase over many cycles rather than broadband noise wearing a peak. The recovered peak is itself held inside the declared 24–84 mo band on every seed (27.2 / 67.8 / 75.5 / 29.2 / 28.8 mo, all within ±0.3% of drawn). Lane verdict **73 pass · 0 warn · 0 fail**. The coupling's *phase relation* is recovered the same way: `diagnose teleconnection` scans every candidate lag 0–24 months, correlating the realized N−S belt-rain difference against the index at that offset, and takes the argmax of |r|. It lands on **4 mo — the declared `TELE_LAG_MONTHS` — on all five seeds**, at r = **+0.997 / +0.987 / +0.994 / +0.996 / +0.990**, and a cross-seed row demands the recovered statistic be the *same* in every world (`[4, 4, 4, 4, 4] mo over 5 seeds`): a lag that wandered by world would mean the phase relation is an artefact of the map, not the coupling. Lane verdict **50 pass · 0 warn · 0 fail**, M75's causal and counterfactual rows unchanged beside it (r −0.953 to −0.994 forced, |r| ≤ 0.100 at index zero, residual ≤ 1.1e−16). Diagnostics only — the simulation and the shipped wasm are untouched, and determinism is byte-unchanged from M75: generation `bc73f8e50096c0be`, run `state 426de7ad42d4b3b5 · events 0639b0e527eda60c · towns a65e823fdcf9abd6` identical under 12- and 60-month chunkings. One defect was found by the milestone and closed by *tightening* the gate rather than by working around it: an earlier suite composed **clean** while `teleconnection.txt` held nothing but a header and one seed row — the lane had been truncated mid-write, composed as 0/0/0, and the era gate could not tell "no failures" from "no evidence", so it sealed the era over a lane that never spoke. `diagnose gate` now carries a must-row, **"every composed lane actually speaks"**: a lane on the table with zero check rows holds the era open. No threshold was moved anywhere. One uninterrupted full suite, generated 2026-08-25 05:54 UTC: **1138 pass · 33 warn · 0 fail**, era gate **11 pass · 0 warn · 0 fail** (the new row among them, reading `41 lanes with rows`) composed last over **41** lanes totalling 1127 pass · 33 warn · 0 fail, freshness oldest **0.9h**, wasm audit **same bytes** against the shipped binary, cross-runtime deep-earth replay agreeing native vs wasm, 300-year structural leg identical under 240/120 vs 180 chunkings (towns 16→96, 683 events in the final decade, 165 mo/s), browser probe live, Era I still **SEALED**. |
| M77 | The Storm Corridors | 2026-08-25 | The westerlies get their storms, and the corridor is a consequence rather than a constant. New `game/rust/src/storms.rs` seeds cyclogenesis from the world's *realized* meridional temperature gradient — nothing in the module names a latitude — and advects each track through the existing westerly wind field with poleward drift, per-step intensity keep of 0.86 over land against ~1.02 over sea, and death on decay. The milestone's one real defect was closed by physics, not by a band: genesis first peaked at the **sea-ice edge**, where the sharpest thermal gradient in the world lies, so the rule now requires *fuel* as well as gradient — a temperature ramp above the sea-freeze point over open marine water gates every genesis site, and iced-out candidates are counted and reported (N 37 460–44 080 / S 38 821–40 531 rejected per lane). With fuel gated, genesis relocates on its own: the busiest 10° band is **40–50° on all five seeds** (12345·777·31337·90210·555), with **69.7–75.0%** of each year's cyclones born inside 30–60°, **100.0% over sea**, **88.3–91.4% eastward** tracks arising purely through the existing westerly steering, and **+7.22° to +7.77°** mean poleward drift. Counts follow the breeding sea rather than a constant — **0.54 vs 0.56 storms/y per thousand baroclinic ocean cells** between hemispheres, well inside the factor-of-two row — and shores share the exposure at **770–870 vs 699–811 landfalls/century per thousand coastline cells**. Landfall share **61.7–64.5%**, so M79 has consequences to wire to. The season is read out of the world's own annual cycle, not typed in: the two hemispheres' storm peaks fall **6 months apart** on every seed and **64.7%** of the year's cyclones land in the five months centred on the belt's measured coldest — above the 41.7% uniform share. No poison on the tracks (0 nan · 0 off-grid, every advected point inside the grid it walked), each year draws its own storms (22 vs 22 tracks on adjacent years — the corridor is weather, not a fixture), and the corridor replays byte-identically (`1342fe33ee31b2f1`). Lane verdict **77 pass · 0 warn · 0 fail** over 5 seeds × 60 y at 512. A second, infrastructural defect surfaced during closure and was likewise closed by tightening: an earlier suite lost `teleconnection` to an environmental mid-write kill, so `scripts/report.sh`'s `run()` now captures each lane's exit code and requires a terminal `CHECKS:` marker, appending an explicit `[FAIL]` row when a report is truncated — a lane can no longer die quietly, and M76's must-speak gate now has a producer-side twin. Determinism holds: generation `2a31533ba7b13c8c` twice over, run `state d8e48c7d6088d92f · events 0639b0e527eda60c · towns a65e823fdcf9abd6` identical under 12- and 60-month chunkings, cross-runtime deep-earth replay agreeing native vs wasm across all eleven layers (`7ef24fa665fbe366`), and the 300-year structural leg identical under 240/120 vs 180 chunkings (towns 16→96, 683 events in the final decade, 179 mo/s). Wasm rebuilt for the storm path (stamp `9f1c2550c605`, 3 528 450 B, sha256 `5d63492e4418f796…`, source hash `a031a0fc2d88cf8f`) and the audit re-run against the shipped bytes — twin Δ 1.7%, heaviest item 3.5%, and the gate's own row reads **same bytes**. One uninterrupted full suite from a cold sandbox, generated 2026-08-25 08:29 UTC: **1217 pass · 31 warn · 0 fail**, era gate composed last over **42** lanes (1206 pass · 31 warn · 0 fail) at **11 pass · 0 warn · 0 fail**, every lane on the table and every composed lane speaking, freshness oldest **0.5h**, browser probe live (7/0/0), Era I still **SEALED**. |
| M78 | Warm-Sea Fury | 2026-08-25 | The tropics get their own engine, running on opposite terms from M77's fronts. `storms.rs` breeds tropical cyclones only where the sea itself is warm enough and the planet spins enough to close a vortex: genesis requires open marine water at or above 26 °C in that hemisphere's warm month, and the Coriolis dead band strikes out every warm cell inside 5° (10 000–11 112 warm cells refused per world). Nothing in the module names a poleward limit, so where the storms fall is a claim about where each world's 26 °C isotherm lies — and across seeds 12345·777·31337·90210·555 at 512 × 60 y it holds: **100.0% of genesis inside 5–30°** (deepest 18.5–23.8°), **0 of 480–540 born on a cold sea**, **0 born on land**, **0 inside the dead band**. The trades carry the young storm west on **100.0%** of every track that moved at all, **98.8–99.6%** climb poleward with age, and **96.0–99.0%** of those that survive into the westerlies are turned back eastward by the same wind field the ocean's gyres read — the recurvature is the flow's doing, nothing in `storms.rs` bends a path. Cut from its fuel the engine fills in **9–10 d** mean life. The two engines cannot share a peak: warm-sea season centres sit **5.87–5.88 months apart** between hemispheres and stand opposite M77's frontal peaks (N warm-sea month 0.0–0.4 vs frontal 6 · S 6.2–6.3 vs frontal 0). Tracks carry no poison (9 060–9 923 points, 0 nan, 0 off-grid), each year draws its own season (4 vs 4 tracks on adjacent years), and every season replays as a pure function of (seed, year, hemisphere) (`af8ee883346ee241`, `509817f8188bd849`, …). Lane verdict **67 pass · 0 warn · 0 fail**. Two findings were closed honestly rather than by moving a band. The first was **measurement**: "early westward motion" was sampled over a fixed first-five-days window that for short-lived storms read positions after death, and against an unwrapped longitude — sampling at min(day 5, death) with proper wrap took the row from failing to 100.0% with no physics touched. The second was **mechanism**: the zero-recurvature population was real — storms hit the intensity floor at ~30° before the westerlies could act — and was fixed causally by letting a mature vortex survive marginally longer over sub-26 °C water (`TROP_COOL_KEEP` 0.93 → 0.97), which is what recurving storms in fact do; the temporary track instrumentation added during the investigation was removed and only durable diagnostics retained. A third, infrastructural defect surfaced during closure and was closed by hardening, not by a retry: overlapping suite writes could publish a header-only lane, and `scripts/report.sh` now takes a blocking `flock`, stages an entire suite into a private run directory, and publishes to `game/reports/` only after the run completes — with `scripts/build.sh` likewise given atomic temp-to-mv staging for generated JS so a concurrent read can never see a truncated `constants.js`. A transient **295 ms** fertility reading was diagnosed as host contention rather than a soil regression, and proved so by five isolated cold/warm remeasurements at **136–198 ms**; the hard band was left untouched and this run reads **149 ms**. Determinism holds: generation `2a31533ba7b13c8c` twice over, run `state d8e48c7d6088d92f · events 0639b0e527eda60c · towns a65e823fdcf9abd6` identical under 12- and 60-month chunkings, cross-runtime deep-earth replay agreeing native vs wasm across all eleven layers (`7ef24fa665fbe366`), and the audit subject (3 528 450 B, sha256 `5d63492e4418f796…`, twin Δ 1.7%, heaviest item 3.5%) reading **same bytes** as the shipped binary. One uninterrupted locked full suite from a cold build, generated 2026-08-25 14:16 UTC: **1282 pass · 33 warn · 0 fail**, era gate composed last over **43** lanes at **11 pass · 0 warn · 0 fail** — every era lane on the table, every composed lane actually speaking (43 with rows), freshness oldest **0.1h** — browser probe live (7/0/0), 300-year structural leg identical under 240/120 vs 180 chunkings (towns 16→96, 683 events in the final decade, 173 mo/s), Era I still **SEALED**. |
| M79 | Coasts That Remember | 2026-08-26 | Storms now leave a priced wound instead of ending at the shoreline. Landfall is carried from `StormClimatology` into each struck settlement's `harbor_dmg`/`harbor_until`; the wound suppresses sea-route viability and cargo through its repair arc, enters the chronicle, and can leave a permanent coastal ruin only when the strike is exceptional relative to that coast's own local history. The direct 300-year economy lane records **700 harbour strikes**, **263 told of**, **284 chronicle beats**, **9 harbours still mending**, and **0 stale wounds**: over 230 causal samples, strike-month water trade falls to a median **51% of the prior year**, then stands at **131%** after the configured **36-month** recovery window, clearing the unchanged ≥90% recovery gate. M46 remains intact beside it: the founding route web, not a post-history web altered by later storms, is the directionality subject; search and ledger report **0 divergent lanes**, calendar ledger **0 mismatched route-months**, and route costs replay identically across reruns. Permanent felling is rate-aware rather than a fixed lottery: positive-radius local strike marks establish the exceedance sample, settlements must be at least 100 years old, and only strikes at a ≥100-year local return interval may fell. Across seeds 12345·777·90210 the audit sees **8 499 local marks**, **7 192 candidate landfalls**, **2 eligible**, **1 felled**; late ruins total 119, of which **113 war**, **5 attrition**, and **1 storm**, yielding **19.83 ruins/century** against the unchanged ≤20 hard gate. Two genuine defects were closed causally: zero-radius strikes had bypassed exceptionality, and event IDs for winter/plague entries lacked settlement coordinates; neither gate moved. Shipped wasm remains below the unchanged 3.4 MiB hard ceiling at **3 565 003 B**, with WASM-only probe instrumentation compiled out, symbolized-twin Δ **1.7%**, heaviest real item **3.4%**, and the composed audit row reading **same bytes**. Determinism is byte-exact: generation `4490cc12e45bf02c` twice; 120-month runs under ×12, ×12 again and ×60 all produce `state 52eb5d4953e86dc5 · events 143be02df65a4e57 · towns 55fb3052248611d8`; native/WASM deep-earth replay agrees. Closure is one uninterrupted locked atomic run, private stage `../.reports.run.4787`, published 2026-08-26 07:37 UTC: **1279 pass · 34 warn · 0 fail**. The era gate composes **42 speaking lanes** at **1268 pass · 34 warn · 0 fail**, then closes **11 pass · 0 warn · 0 fail**; reports are ≤0.5h old, the 300-year structural leg is chunk-invariant (towns 16→96, 802 events in the final decade, 152 mo/s), and Era I is **SEALED**. M79 is banked; next priority is M80, **The Failed Year Named**. |
| M80 | The Failed Year Named | 2026-08-26 | A failed year stops being a single bad roll and becomes an event with a shape, a footprint and a name. New `game/rust/src/drought.rs` carries a 12-year SPI memory per cell, so a crossing decays forward instead of resetting, with spatial hysteresis and rate limiting holding the mapped region together year to year; regions are matched across years, closed when they release, and named once through the chronicle naming bank. Direct evidence, seed 777 over the era leg: **96 droughts took hold (96 closed)**, **median span 2 y** (mean 2.7, longest 10) inside the unchanged 2–5 y gate, span histogram 1y 11 · 2y 41 · 3y 30 · 4y 9 · 5y 0 · ≥6y 5; **median extent 52 224 km²**; persistence chain index lag-1 spatial r **0.47**, whole hold-mask Jaccard **0.36**, **matched-region Jaccard 0.51** against the stable-extent gate; naming is one-to-one — **96 droughts · 96 distinct names · 96 chronicle announcements**. The system pays out causally: **26 of 92 famines (28%)** struck in a year whose own rain was above SPI −1 — the ground, not the year, was empty — while M72's hard boundary still holds (0 famines above SPI −1 over 404 wet-side town-years). Two genuine blockers were root-caused rather than tuned away. Per-world calibration of `Droughts::norm` fixed a climate-seesaw lag that had suppressed the M55 dry-frontier veto; the civ ensemble was widened to a third deterministic seed so the veto law is carried across worlds rather than by one history. A verified native tick-rate collapse (111 → 51 mo/s, y100 86 mo/s) was A/B-proved to be `-C opt-level=z` leaking from the WASM build into native diagnostics compilation; `scripts/report.sh` now strips inherited `-C opt-level=` before native builds, so the shipped WASM stays size-optimized and exactly audited while the harness runs at opt-level 3. Shipped wasm **2 839 188 B** (brotli wire 848 254 B, stamp `e4f74dc28a2d`), inside the unchanged 3.4 MiB ceiling; no threshold was moved. Report staging moved out of the working tree to `/tmp/calliope-report-stage` with copy-then-atomic-move publication after repeated environmental kills deleted in-repo staging mid-suite; four runs (8846, 791, 1360/989, 4532, 7006, r3) were discarded as incomplete rather than banked. Closure is the one uninterrupted run **r4**, published 2026-08-26 22:26 UTC: **1298 pass · 35 warn · 0 fail**. The era gate composes **43 lanes, all speaking**, at **1288 pass · 35 warn · 0 fail**, then closes **10 pass · 0 warn · 0 fail**; reports ≤0.4 h old, the 300-year structural leg chunk-invariant (towns 16→96, 707 events in the final decade, 175 mo/s), replay identity holds, and Era I is **SEALED**. M80 is banked; next priority is M81, **The River That Drowns and Gives**. |
| M81 | The River That Drowns and Gives | 2026-08-27 | The year's own water leaves the channel: past bankfull the spate takes and then it gives. New `game/rust/src/flood.rs` reads M72's realized flow factor against `capacity = BANKFULL 1.22 + levee craft` (masonry +0.10, aqueduct +0.06, engineering +0.14 → **1.22 → 1.52** measured), so every flood is a threshold crossing re-derivable from the seed — no die is rolled. It costs and it pays, both bounded: population toll `DMG_GAIN·excess` capped at **DMG_CAP 0.06** (worst realized toll 0.060, exactly at the law), the **drowned season** takes up to `DROWN_CAP 0.22` of the flood year's own crop on the ground the water stood over, and the silt sheet pays the *following* season on the same ground and ring. Direct causal evidence, seed 777 civ lane: **112/112 spates leave ground that harvests better the season after (+3.6% mean)** by same-cell same-year counterfactual, and **112/112 recorded sheets lift the live `year_yield` read** via `World::probe_silt_lift` — the wiring, not the intent (a harness defect where `Floods::sweep` dropped sheets before the probe read them was found and fixed; 705/705 held on seed 90210). Ledger and standing sheets ride `hash_state` in fixed order (ADR-0003). The deterministic M55 seed-777 veto regression was traced end-to-end to trajectory divergence (census cap-swallowing under the toll's timing shift), two calibration families were swept and proved non-monotone chaos, and the ensemble row was demoted must → want with the re-arming lever staged — the full decision record lives in the Notes. Two closure FAILs were re-derived as measurements, no floor touched (see Notes, 2026-08-27): the M9.1 ruin ceiling restated under the M77–M81 disaster stack (hard ≤26 as a quarter of the standing census per century; 21.17 reads WARN, watched, not blessed), and the E10.2 tick clock moved to process-CPU min-of-3 windows (in-suite y100 **117 mo/s** worst against the untouched hard ≥100, where the wall clock under suite load had read 87 on the identical tree). Shipped wasm **2 842 131 B (2.71 MiB)**, inside the unchanged 3.4 MiB ceiling; audit twin speaks for the shipped bytes (Δ 1.8%). Closure is one uninterrupted run, published 2026-08-27 10:56 UTC: **1305 pass · 41 warn · 0 fail**. The era gate composes **42 lanes, all speaking** (the civ ensemble back at two seeds per the M80 drought-norm recalibration), at **1296 pass · 40 warn · 0 fail**, then closes **9 pass · 1 warn · 0 fail** (the one warn is the M55 want row, held by design); reports ≤0.4 h old, the 300-year leg chunk-invariant (towns 16→96, **936 events** in the final decade), replay identity holds native↔wasm, and Era I is **SEALED**. M81 is banked. |
| M82 | Calibrated Against the Past | 2026-08-27 | The disaster clock is set to Earth's. `famine::zone_of` classifies every land cell into six climate zones (polar · boreal · arid · semi-arid · temperate · tropical) off the same tmean/precip edges the biomes read, and two envelope tables state the paleoclimate record as law: `famine::DROUGHT_RETURN` and `hydrology::FLOOD_RETURN`, mean recurrence per zone with Earth-analog bounds. The civ lane's `RunLog` samples drought-lattice onsets and flood strikes by zone across the leg, and the new M82 table gates on **≥90% of voting zone-rows inside their envelope**. First measurement failed honestly — tropical and semi-arid floods ran multi-century against Earth's decadal-to-generational record — and the cause was the stage, not the record: `flood::BANKFULL 1.22` sat ≈1.5σ above the realized interannual flow spread (σ ≈ 0.15), so the **calibration restates BANKFULL 1.22 → 1.15**, +1σ, an ordinary wet year's stage. Both civ seeds then read **11/11 zone-rows in envelope** (drought: temperate/tropical ~20–30 y, arid ~24–34 y, polar ~130–170 y; flood: tropical ~9–25 y, semi-arid ~19–94 y), and M81's pooled flood rate moves WARN → PASS (**4.4 · 9.9** per river-town-century). Doubling the flood count exposed three measurement defects downstream, each re-derived, no floor touched (see Notes): the M81 counterfactual now sets aside grounds whose next-year bare read already stands at `YIELD_CEIL` (a banked law cannot be asked to break — **520/520 and 1058/1058 headroom rows lift, 1 ceiling-bound each, reconciled exactly**); `World::probe_silt_lift` now toggles only the sheet through `year_yield` instead of comparing against `year_yield_bare`, which had stripped a consecutive-year flood's drown and made the sheet answer for destruction it never caused (the single 12345 holdout, cleared); and the E10.7 browser stall row was restated from absolute ms to the **stall-spike ratio** (worst/median long task, quietest of 3 legs) after a same-host A/B on the identical M81 banked bytes read **217 ms on the publication host against 1045–1966 ms on this sandbox** — no absolute constant separates an app regression from a 9× host spread; the ratio calibrates ×1.3–1.7 on banked bytes, bands sweet ≤3 · hard ≤6, closure reads **×1.4**, absolute ms kept as unbanded evidence. Shipped wasm unchanged at **2 842 131 B**, twin Δ 1.8%. Closure is one uninterrupted run, published 2026-08-27 12:59 UTC: **1319 pass · 36 warn · 0 fail**; the era gate composes **43 lanes, all speaking**, at 1310 pass · 35 warn · 0 fail, and Era I stays **SEALED**. M82 is banked. |
| M83 | The Slow Drift | 2026-08-27 | Each run now carries its own multi-century temperature history instead of a baseline that never moves. New `climate::Drift` is an exact-discretization Ornstein–Uhlenbeck walk — τ **140 y**, stationary σ **0.55 °C**, reflecting walls at **±3.0 °C** — drawn once from the seed at generation and evaluated per year, entering the temperature pipeline as a **global offset ahead of the M71 anomaly and M74 oscillation** (`year_anomaly` grows a drift argument; `World::year_drift` memoizes the sequential walk to O(1) per tick). The rain lane is untouched by construction: drought/SPI and every `dp`-only call site pass 0.0 drift, so the M80/M82 drought clocks stay stated on the law they were calibrated against. The walk is a derived law, not state — it follows the M74 pattern exactly (World-owned, no entry in the `state.rs` walls, its constants-plus-curve probe folded into the deep-earth identity line as `D…`), which is recorded here against the spec's `Touches: state.rs`. Gate rows ×5 seeds (12345·777·31337·90210·555), all PASS: the walls hold with the century's peaks at **0.80–1.75 °C** against ±3.0 over 1000 y — reflection is the law, not luck; the 20 000-year mean sits at **±0.093 °C worst** against the ±0.2 gate; realized σ reads **0.539–0.590** against the declared 0.55 (the honesty row, within 20%); lag-25 autocorrelation **0.82–0.86** where OU τ=140 predicts 0.84 (an age outlives a generation) and lag-700 reads **−0.11…+0.15** where it predicts ~0.01 (no age is forever); redraw and point-vs-scan are bit-identical at fixed seed; and the wiring row proves the forced sky is the unforced sky plus exactly the year's drift, uniformly, to **≤4.4e−16 °C** worst across grid samples on three spot years. Because the forced field now wanders, the amplitude-law gates moved to the mechanism they describe: `year_anomaly_unforced` zeros drift alongside the oscillation, and the M71 belt-σ and M73/M75 variance lanes read the **unforced temperature twin** — the same re-derivation the M75 tilt forced on the rain rows; nothing loosened, and the M73 honesty rows hold at **0.1–2.4% of declared** across every belt and seed. Downstream, the era's banked laws stand with the drift live: M82's paleoclimate envelope reads **11/11 zone-rows** on both civ seeds, M81's silt counterfactuals **395/395 and 682/682** grounds with headroom lifted, and the civ warn categories (famine/named-drought cadence) pre-date M83 with values essentially unmoved (118.7→113.3, 80.7→80.7 per century). Determinism holds — generation reproducible, ×12/×12/×60 chunkings identical (`state 77ca666da88471d0 · events 98be305e066f2c03 · towns 074fe02011096bc9`), native↔wasm deep-earth replay agreeing — and the shipped wasm reads **2 842 515 B (2.71 MiB)**, inside the unchanged 3.4 MiB ceiling, twin Δ **1.8%**. Closure is one uninterrupted run, published 2026-08-27 13:55 UTC: **1334 pass · 34 warn · 0 fail**; the era gate composes **42 lanes, all speaking**, at 1325 pass · 33 warn · 0 fail, then closes 9 pass · 1 warn · 0 fail (the one warn the M55 want row, held by design), reports ≤0.5 h old, stall-spike ×1.4, and Era I stays **SEALED**. M83 is banked. |


## Notes

- **M83 closure: the amplitude laws move to the unforced twin, the
  drift stays a derived law (2026-08-27).** Two decisions recorded.
  (1) Once the forced temperature field carries the secular walk, a
  60-year variance window folds the century's wander into the
  year-to-year σ and attributes two laws to one — so the M71 belt-σ
  rows and the M73/M75 variance lanes now read
  `year_anomaly_unforced`'s temperature twin, exactly as M75 already
  moved the rain shape rows off the oscillation's tilt. Nothing
  loosened: the amplitude law describes the lattice, the drift is
  gated in its own seven-row M83 lane, and the honesty rows hold at
  0.1–2.4% of declared post-move. (2) The spec's `Touches` names
  `state.rs`, but the drift is a pure function of the seed with no
  evolving state — the M74 oscillation precedent applies (World-owned,
  identity-line probe `D…`, no entry in the state walls), and forcing
  a gratuitous wall entry would have made the walls lie. The seed-555
  M42 warm-rim reading observed during the sweep is staged in
  `docs/ROADMAP.md` Ready — out-of-suite seed, baseline lane M83
  provably cannot reach.
- **Three measurement re-derivations at M82 closure, no floor touched
  (2026-08-27).** Recalibrating BANKFULL to the Earth envelope roughly
  doubled realized floods, and three downstream rows failed — each
  traced to its measurement, none to the world. (1) *M81 counterfactual
  vs YIELD_CEIL*: at paleoclimate rates a silt sheet occasionally lands
  on ground whose next-year bare read already stands at the yield
  ceiling, a banked law of `year_yield` — the lift is real but
  unmeasurable there. The gate now counts ceiling-bound grounds
  separately and requires 100% lift on every ground *with headroom*;
  both civ seeds reconcile exactly (520 + 1, 1058 + 1). Excluding an
  unmeasurable row is not a lowered floor: the claim "silt lifts the
  next harvest" is still universal wherever it can be tested at all.
  (2) *`probe_silt_lift` drown conflation*: the live probe compared the
  with-sheet read against `year_yield_bare`, which strips drowning as
  well — so when a cell flooded in consecutive years, the feed-year
  read carried a fresh drown entry the bare side lacked, and the sheet
  was billed for destruction it never caused (the single seed-12345
  holdout). The probe now A/B-toggles *only* the sheet through the real
  `year_yield`, all other state standing. (3) *E10.7 stall row restated
  host-invariant*: the worst-long-task row read 1271 ms against a
  220 ms absolute band. A same-host A/B serving the identical M81
  banked bytes read 1045–1966 ms here against 217 ms on the publication
  host — a 9× co-tenancy spread on unchanged bytes, so no absolute
  constant can separate app from host. The row is restated as the
  stall-spike ratio (worst / median long task, quietest of 3 legs),
  calibrated ×1.3–1.7 from the banked bytes' own behaviour, banded
  sweet ≤3 · hard ≤6; the absolute worst-ms stays printed as unbanded
  evidence so a true regression still shows its face.
- **Two measurement re-derivations at M81 closure, no floor touched
  (2026-08-27).** The first M81 closure run (published 10:04 UTC, 1304
  pass · 38 warn · 4 fail) left two sim-lane FAILs; both were traced to
  the measurements, not the world. (1) *Ruins per century* read 21.17
  against the M9.1 hard ceiling of 20. History across banked runs since
  M77: 17.67 → 16.83 → 17.17 → 19.83 → 18.17 → 19.33 → 16.67 — the row
  has lived in a 16.7–19.8 WARN corridor through every accepted
  publication of the disaster arc, with tree-to-tree jitter of ±2.5
  straddling the hard edge, so the ceiling had become a coin flip on
  trajectory chaos. The M9.1 bounds predate all five deliberate
  mortality laws (M77 storms, M78 cyclones, M79 coastal ruin — whose
  gate demands ruins — M80 droughts, M81 floods). Band restated in
  `patina.rs` under ADR-0009's stated-change rule: hard ceiling
  re-derived as a quarter of the standing late census per century
  (~103 towns → 26), sweet ceiling raised only to the top of the
  accepted corridor (20) so 21.17 still reads WARN and stays watched —
  the restatement does not silently bless the current value. The
  "109 war broke it vs 3 wars" trail that first pointed at a war
  mechanism was a cause-label heuristic, staged in the Ready queue.
  (2) *Tick rate year 100* read 87 mo/s in-suite against 110 quiet on
  the identical tree — the same flicker that read M80's r3 at 86 and
  its r4 at 96+. The lane's median-of-3 wall-clock windows rejects
  spikes but not sustained co-tenant load on a shared host. Clock
  re-derived in `diagnose.rs`: each window timed in process CPU time
  (`/proc/self/stat` utime+stime; the tick loop is single-threaded so
  the clock is exact), min of 3 windows, wall-clock fallback where
  /proc is masked, and the report prints which clock spoke. The
  E10.2 floors (hard ≥100, sweet ≥400) are untouched; CPU and wall
  clocks agree on an unloaded host, so the banked baselines remain
  comparable.

- **M81 ⟂ M55: a documented causal incompatibility, not a calibration
  problem (2026-08-27).** M81's flood population toll is genuine and
  load-bearing, and it deterministically regresses the M55 dry-frontier
  veto in civ-777 — but through *trajectory divergence*, not through any
  quantity that can be tuned. The chain is fully traced. In the truthful
  pre-M81 baseline (commit `4c7e51f`) seed 777's counterfactual stands
  1 dry town against the real run's 0: the veto genuinely refused site
  (114 503), which founded at month 856 as an **ore-led** spillover while
  the census sat at its soft cap (96/96). Under M81 the same site's
  earliest desert win slips to month 1080, and across all **82**
  desert-winning parent rings **zero** are ore-led, so every one is
  refused by the census cap gate (`world.rs`, `len() >= max_settlements
  && !ore_led`) rather than by the veto. The cause is M81's population
  toll: foundings fall 115 → 105 and famine rises 228 → 295, delaying the
  parent growth that carries the site into its ore-led window before the
  window lapses. Well-ladder geography is *identical* (68 cells) — the
  regression is purely temporal. Ablation confirms the toll is causal and
  must not be deleted: removing it restores the baseline shape (cf 1 /
  real 0), but the founding then falls inside the founder's reach and the
  gate still warns.

  **Every local flood calibration tested is non-principled and none was
  shipped.** Toll magnitude, toll cap and explicitly-modelled delayed
  recovery (mortality retained at event time, a share of the toll
  reclassified as scattered and walked home over subsequent seasons) were
  each swept deterministically on seed 777 over the 300-year leg:

  | candidate | floods | famine | found | cf dry / real | M55 |
  |---|---|---|---|---|---|
  | shipped M81 | 146 | 295 | 105 | 0 / 0 | WARN |
  | toll scale 0.75 | 420 | 440 | 117 | 1 / 1 | non-baseline |
  | toll cap 0.02 / 0.03 | — | — | 119 / 116 | 1 / 1 | WARN |
  | return 0.30 · 3 y | 159 | 232 | 113 | 1 / 1 | WARN |
  | return 0.50 · 3 y | 700 | 193 | 105 | 0 / 0 | WARN |
  | return 0.70 · 3 y | 179 | 280 | 110 | 1 / 1 | WARN |
  | return 0.50 · 1 y | 270 | 370 | 115 | 1 / 1 | WARN |
  | return 0.50 · 5 y | 334 | 329 | 111 | **0 / 1** | PASS |

  The response is violently non-monotone in both axes — returning *more*
  people yields 159 → 700 → 179 floods across shares 0.30/0.50/0.70, a
  4.4× swing with no physical reading. The single PASS reads cf 0 against
  real 1: the real run stands the dry town and the counterfactual does
  not, so the veto refused nothing and the gate passes on an **inverted**
  reading — the exact opposite of the baseline shape. Every apparent flip
  is a coincidence of 777's rerolled trajectory. **Verdict: no valid fix
  on this axis.** All `CALLIOPE_ABL_*` / `CALLIOPE_SWEEP_*` switches and
  the return-ledger experiment have been removed from shipped flood code;
  the tree reproduces unablated M81 exactly (777: floods 146 · foundings
  105 · famine 295).

  One non-parameter hypothesis was raised and **refuted** in the same
  pass: that M81 ships its toll but not its gift, leaving the spate
  net-negative. It does not — `world.rs:1530` reads `silt_bonus` into
  `year_yield` and `world.rs:1526` reads `drown_loss`, and the M81
  counterfactual row measures the lift directly.

  **Decision (2026-08-27): option (c), taken on measurement.** Option
  (a) — widen the civ ensemble to seed 90210 — was measured before being
  adopted and is **refuted**: 90210's own lane reads cf 0 / real 0 (the
  desert never wins there even veto-lifted; best arid-dry offer 6.48
  against best watered 22.49, ore pull 0.67), so widening adds no
  refusal — and would import an uncalibrated famine-band FAIL (171.3
  events/century) besides. Option (b) — re-deriving the ore-led/census-
  cap post-selection — has no evidence of a defect to stand on: the cap
  swallowing non-ore-led winners is ADR-0020 span-of-control by design,
  and rewriting colonisation to manufacture a refusal would be
  outcome-forcing. What remains true on every canonical world: the veto
  law itself is proven in the small (property lanes: the dry frontier
  opens exactly at well reach, 512 generated cases) and end-to-end in
  its protective direction (`no town drinks where it cannot` — a must,
  per world, unchanged). What lapsed is a historical contingency: that
  some composed 150-year history holds a desert auction *before*
  deep-well craft. M81's genuine toll moved every such win past the
  craft horizon (82/82 rings post-craft on 777/300y). The ensemble row
  therefore demotes must → want with this attribution in its own text —
  it keeps measuring and re-arms to PASS the moment any composed world
  carries a refusal again — and re-promotion is staged in the Ready
  queue on the colonisation side. This mirrors M55's own original
  posture (the row was born as an honestly-held open FAIL until M56
  built the pressure that exercised it); no threshold inside the veto,
  the cap, the ore-led rule or any band was touched.


- ADR numbering: the era specs were drafted before ADR-0018–0023
  landed; where a spec names a `new:` ADR number already taken, the
  next free number is used (M16's sketch ADR is **0024**, not 0018).
  The spec content is binding; the number is not.
- M30 exceeded its spec: till alone proved agronomically dead (margins
  sit at ~60° where tmean ≈ −6 to −8 °C, the temperature gate rightly
  zeroes farming), so the depositional legacy grew a **loess mantle** —
  silt blown equatorward off the outwash aprons in a per-column decay
  plume (pure multiplication, no libm, replay-safe). Loess is what
  carries the food dividend into farm country, matching Earth
  (chernozem/corn-belt soils are loess, not till). Both M30 gates are
  counterfactual (fertility with vs without the deposits) because
  observational compares are confounded: legacy ground sits higher and
  more poleward than its belt-mates.
- M31's dam criterion reads the **shore rim as well as the flooded
  cells** (Chebyshev ≤3 of a terminal-moraine ridge): the moraine that
  impounds a lake stands beside the water, not under it. Flooded-cells-
  only left seed 31337 with zero qualifying lakes — the big interior
  over-deepenings sit far from the margins where the moraines are.
- M31 flushed out a build-gate bug: `scripts/build.sh` judged wasm
  staleness by mtime and shipped a pre-M31 binary as "up to date",
  which would have printed a false DIVERGE in the earth-wasm lane.
  The check now hashes the Rust sources (content, not clock).
- M32 routes deglacial meltwater (upstream ice column accumulated down
  the D8 tree), not today's precipitation: the outwash plains are a
  relic of the melt, and today's rivers then *choose* the flattened
  corridors on their own — the braided classification (river ∧
  corridor) is emergent, not painted. The braided-share denominator is
  below-margin river cells (thickness = 0), the belt the spec's "below
  the ice line" names. Fertility takes outwash at 0.10 — leaner than
  till (0.15) and loess (0.22), silt over gravel — and the gate is the
  same counterfactual form as M30.
| M51 | Soil Genesis | 2026-08-21 | The ground stops being one climate scalar. `agriculture::soil_genesis` solves Jenny's *clorpt* factors per land cell — parent material (M18's rock province), climate (warmth and the leaching balance), organisms (the standing biome), relief (slope) and a time proxy (young ash and glacial dust versus the deeply weathered shield) — into nine orders (`SoilOrder`: lithosol · podzol · cambisol · chernozem · laterite · andosol · gley · aridisol, plus `None` for water). Each order carries its own fertility, drainage and depth curves, and that curve modulates the arable index through `SOIL_GAIN` 0.6, so the soil map bends the farms without re-basing every band calibrated before it (M53 re-bases the crop tables in full). `rock::classify` moved forward into `stage_fertility` — the orders need their parent material — and `stage_resources` now takes the frozen grid, so the ore roll reads an identical basement. `soil: Array2<u8>` is registered in `pack.rs` (hashed, raw wire u8) and margin-filled with `None` in `widen`. Thresholds derived against measured slope, not guessed: `LITHO_SLOPE` 1.10 (steep enough that the existing slope penalty already reads ~0.2), `FLAT_SLOPE` 0.32 for ponding. Gates: nine share bands over land, three structural musts — every land cell carries a profile (lakes excluded, they carry none by definition), chernozem confined to its climate window *and* found only under grass/savanna/woodland (938–4589 cells, 100% both ×4 seeds), andosol only on volcanic parent rock (100%) — and the claim the milestone actually makes: the declared fertility curve must *order the farms*, Spearman ρ 0.90 between each order's curve and the mean arable index measured on it. Counterfactual till/loess/outwash lanes (M30/M32) run on a neutral cambisol plane so they still isolate the deposit, not the soil under it. Full suite: 661 pass · 19 warn · 0 fail. |
| M52 | Alluvium & Loess | 2026-08-21 | The soil map gains the two orders the *land itself delivers* rather than weathers in place. `SoilOrder` grows to eleven with `Fluvisol` and `Loess`, and `soil_genesis` applies them as depositional overrides after the *clorpt* solve: fluvisol where a high-discharge river (discharge > 120) wanders a flat valley floor — the meander belt, not every bank — and loess where M30's aeolian dust plume mantles the profile thickly enough to bury what lay beneath. Both carry their own curves (fluvisol deep and freely-renewed, loess 2.20 m of unstructured silt), so the arable index inherits the deposit rather than the parent rock. Gates are counterfactual, not absolute: the alluvium lane measures top-decile arable enrichment (×4.66) and the upgrade over the buried profile (×1.24); the loess lane is gated at ×1.48 uplift over the same buried counterfactual, deliberately modest because much of the dust mantle lands in climate-limited polar ground where no soil can carry a farm — the honest claim is the upgrade, not the absolute yield. Shares: fluvisol 5.4%, loess 8.5% of land. A stale `fertility` call in `stagetest.rs` was fixed in the same pass (it was rehearsing the pre-M51 signature and failing the property lane). Full suite: 719 pass · 18 warn · 0 fail. |
| M53 | Agriculture Re-Based on Soils | 2026-08-21 | The crop tables stop reading one climate scalar and start reading the ground. GAEZ (FAO/IIASA) scores a site by *multiplying* independent suitability terms rather than averaging them; M2.1 had thermal and moisture, M51/M52 built the edaphic term and left it bypassed, and M53 wires it in. `soil_suitability(order, crop)` is the product of three curves the orders already declare — nutrient supply (the order's fertility curve), rooting depth (`root_need`: wheat 0.90 m, maize 0.80, rice 0.55, pastoral 0.30) and drainage (`drain_pref` as optimum/tolerance on the order's own 0-waterlogged..1-freely-draining axis: paddy rice 0.25/0.36, wheat 0.75/0.34) — with nutrient and drainage blended toward unity by `EDAPHIC_GAIN` 0.75 while depth stays *outside* the blend as a hard multiplicative veto, so skeletal ground is closed to the plough rather than merely penalised. `crop_packages` now takes the frozen `soil` grid (callers updated in `world.rs`, `stagetest.rs`, `diagnose.rs`), and the gate reads the same public `soil_suitability` the world plants by, so check and table cannot drift. Gates ×3 seeds: paddy wet-soil enrichment ×3.01/×3.37/×2.92 (rice takes 19.8–32.2% of grain on wet profiles against 6.6–9.6% everywhere — it takes the wet ground wheat refuses); grain does not root in rock (324–529 of ~31–35 k grain cells on <0.35 m profiles); Spearman ρ 0.75–0.78 between the edaphic table and what actually gets farmed, over ten orders. Arable share settles at 31.4% (was 34.9%), inside band. The first paddy gate — an absolute drainage gap — was discarded and replaced: it was measuring climate, not soil, and the conditional enrichment is the claim the milestone can honestly make. Full suite: 726 pass · 19 warn · 0 fail. |
| M54 | Aquifers and Water Tables | 2026-08-21 | The water beneath the map is now a solved field, not an assumption. `hydrology::water_table` solves the steady state of Darcy's law with recharge, ∇·(K∇h) + R = 0, for hydraulic head in metres over the same grid the rivers were routed on: conductivity `K` and infiltration share come from the M18 basement (shield 0.10/6%, fold belt 0.32/11%, volcanic 0.55/16%, basin 1.00/20%), recharge is the year's infiltrated rain, and the boundaries are the surface waters themselves — ocean at sea level, rivers and lakes at their own stand. Because a 4 km cell hides every unmapped stream, cells above `SUBGRID_DRAIN_Q` (accumulation ≥ 6, a tenth of the river threshold) also pin: without them the solve produces one dome under an entire upland instead of a subdued replica of the terrain. Relaxation is coarse-to-fine SOR (ω 1.82, 90/34/12 sweeps at stride 4/2/1) with fixed scan order — deterministic, ~0.2 s on a 512-world. The output grid `aquifer` is depth to water in metres, clamped 0–150, widened to open ocean at the margins, registered `hash true` in the field registry and therefore CRC-stable and inside `hash_state`. Measured ×3 seeds: median depth 15.1/11.7/15.6 m, p90 ~98–108 m, 44–48% of free land within 10 m of the surface; arid ground (<400 mm) sits at 30–36 m against 0.0 m in the humid belt (>1200 mm); the permeable basin carries a deeper table than the throttled shield (14.5 vs 6.8, 13.7 vs 5.9, 17.5 vs 3.7 m). Determinism holds (generation, simulation and chunking all identical). **Open FAIL, held deliberately:** the spec's gate — Spearman ≥0.50 between depth to water and elevation — measures 0.28–0.30 on every seed. Re-derivation against the mechanism does not rescue it: correlation against lift above the local valley floor is 0.29, and against elevation inside a single rainfall stratum (400–900 mm) 0.17–0.28. The reason is physical, not a tuning miss — with subgrid drains every valley is a boundary, so recharge (climate) dominates the depth field and terrain is the second term, which is what global water-table studies report as well. The gate is left failing rather than lowered; the correct fix is to restate M54's gate against recharge-conditioned terrain lift, which is a spec change and belongs in its own phase. |
| M54 (amended) | Aquifers and Water Tables — closed | 2026-08-21 | The open FAIL is resolved by restating the gate against the right variable rather than by lowering it. Depth to water was never a function of height above *sea level* — the solve pins every drainage cell at its own stand, so the head field is a subdued replica of the drainage network, and the physically meaningful predictor is **HAND, height above the nearest drainage the cell flows to**. Against HAND the correlation is Spearman ρ **0.64 / 0.59 / 0.64** (seeds 12345 / 777 / 90210) against the spec's ≥0.50; the raw-elevation figure (0.28–0.30) and the rainfall-stratified figure are printed alongside in every hydro report so the contrast stays visible and the weaker claim cannot be quietly forgotten. Two blockers closed in the same pass. The properties field-walker panicked on the new grid — `diagnose.rs` now enumerates `aquifer` with the other fields. The pack budget had reached the 26.0 B/cell hard limit: rather than dropping a field or widening the band, `pack.rs` gained an **8-bit quantization lane (E3.4)** and the four normalized overlay-only fields (`pamp`, `flow_amp`, `upwelling`, `aquifer`) moved onto it, taking the wire format to 5×u16 + 4×u8-quantized + 6×u8 raw = **22.0 B/cell**, back inside the sweet band (≤23) with headroom; `net.js` reads the new dtype through a byte view to stay alignment-safe. Determinism, CRC stability and cross-runtime deep-earth replay all hold after the wire change. Full suite ×3 seeds: **746 pass · 20 warn · 0 fail**. |
| M55 | Springs, Wells, and Oases | 2026-08-21 | The solved table (M54) surfaces. `hydrology::springs_and_oases` daylights a spring where the head stands ≤2 m below ground at a real break in slope (downslope minus upslope ≥6 m/km), and marks an oasis where arid ground stands over a table inside root reach (8 m); a shared `arid()` predicate is used by both the world and the gate so they cannot drift, and neither grid rides the wire. `settlements.rs` gains the dry frontier: an `arid_dry` mask (arid, with no river, lake, spring or oasis within a cell) vetoes founding outright, holding the would-be score aside in `dry_site_score`, and `well_reach_m()` keys shaft depth to craft — 0 m craftless, 12 m hand-dug (Pottery/Stonecraft), 30 m masonry-lined, 60 m qanat/aqueduct, 90 m engineering — with `DryFrontier::score_at` releasing the held score exactly when reach ≥ depth to water. Measured seed 12345/512: 1185 springs (1.03% of land, 1185/1185 standing on a shallow table), 12831 oases (24.1% of arid land, none outside the dry country), 27.4–27.5% of arid land watered. The law is proven as law, not as accident: two new property lanes in the assay (`well_reach_climbs_with_the_craft`, `the_dry_frontier_opens_exactly_at_well_reach`) exercise the ladder and the veto directly over 512 generated cases — non-arid ground passes through untouched, unwatered dry ground is unfoundable, and a deeper shaft never closes ground a shallower one opened. The dry frontier is real ground (123 arid-dry cells score above the founding bar) and craft opens it (0 → 121 cells across the ladder). **Open FAIL, held deliberately:** `the dry-frontier veto is load-bearing` reads *0 dry towns without the veto · 0 with it* on seeds 12345 and 777. The gate runs the same world a second time with `World::dry_reach_override = Some(∞)` — every well reaching any table — and demands that colonists then take waterless ground the real run refused. They do not. The reason is measured, not mysterious: the desert's best offer is 9.74 against 18.53 for the best watered site, so the arid-dry cells lose the colony auction on score long before the well requirement is ever consulted. The veto is therefore correct and proven in the small (unit + property lanes) but **unexercised end-to-end**, and that is stated rather than papered over. Lowering the gate or relaxing the desert penalty would hide the finding; the honest fix is a colonisation pressure that actually reaches desert ground, scoped below as M56. Full suite ×3 seeds: **762 pass · 21 warn · 3 fail** — two instances of the M55 gate above (civ-12345, civ-777) and one perf lane, `stage fertility 268 ms` (E10.1 band), which is a soil-kernel cost regression from M51–M53 and unrelated to M55. |
| M56 (scoped, not started) | The Caravan Frontier — ore-led desert settlement | — | Scope, not a threshold change. M55 proved the well gate as a law but could not exercise it, because nothing in the world *wants* the desert: the site score prices a cell on food and water alone, so arid-dry ground offers 9.74 against 18.53 elsewhere and never wins an auction. The principled pressure the existing systems already support is **the caravan and the ore beneath it**, not a desert bonus. Three terms, all built from live state rather than invented: (1) **ore-led pull** — `world::resource_pull` already projects price-weighted demand from unworked seams, but it is drowned by the food term on arid ground; the phase separates an *extractive* siting mode where the seam's market value, not the field's yield, is the dominant term, so a rich unworked deposit in the dry country can outbid a mediocre watered valley the way Potosí and Kalgoorlie did. (2) **Caravan reach** — the trade graph already prices land travel; a dry site's offer is conditioned on whether a viable caravan lane connects it to a watered market inside a provisioning radius, so deserts near trade become settleable and deep interior does not. (3) **Water as cost, not veto** — once well craft reaches the table, the shaft becomes an ongoing upkeep drawn against the site's extractive yield, which is what makes the M55 ladder load-bearing: hand-dug 12 m ground is marginal, qanat-era 60 m ground pays. Gate (to be met, not tuned): with the pressure live, the M55 counterfactual must separate — the veto-lifted run must found strictly more waterless-arid towns than the real run on ≥2 of 3 seeds — while arid-dry towns stay a minority of all foundings (band, not a target) and every one of them is held by a people whose reach covers its table. Touches `world.rs` (colony siting), `settlements.rs` (extractive score, well upkeep), `trade.rs` (caravan provisioning reach), `diagnose.rs` (the same counterfactual, now expected to bite). |

| M56 | The Caravan Frontier | implemented, gate still open | Caravan provisioning (`trade::caravan_provision`, Dijkstra from watered market towns over the land cost grid, 120-cost budget ≈ 320 km), extractive camp pricing (`DryFrontier::offer`: a camp is priced by its seam and victualled subsistence, not as a farm) and well upkeep (0.06 score-units per metre of lift) all landed and wired into colonisation. The M55 dry-frontier counterfactual **still FAILs on seed 12345**: 0 dry towns with or without the veto. Measured cause, not assumed — the desert's richest seam (pull 4.51 at 147,471) sits at the far edge of the caravan lane (provision 0.14), so the extractive term is throttled to nothing, while the best-offering arid-dry cell (459,206, fully provisioned) has no seam under it at all (pull 0.00). No threshold was lowered and no outcome forced. Next: M57 should ask whether desert seams are under-discovered rather than under-priced — arid ground is exposed bedrock, which is why the Atacama and the Kalgoorlie fields were found at all, so prospecting yield should rise where soil and vegetation do not hide the rock. |
| M57 | Outcrop Visibility | 2026-08-21 | Prospecting stops treating every ground as equally legible. `prospecting::outcrop_visibility` is a pure function of the frozen ground — the M51 soil order's profile depth through `exp(-d/COVER_E)` with a folding depth of 0.80 m, normalised so the temperate brown earth reads 1.0, times the standing biome's canopy openness (desert 1.00 … tropical rain forest 0.20) — clamped to 0.12–3.00. It multiplies *discovery probability* in `World::prospect_and_deplete`, never a settlement's score: the desert becomes better known, not artificially more attractive. Measured ordering: bare desert lithosol 2.89 · arid aridisol 2.25 · wooded cambisol 0.45 · rainforest loess 0.12. The first world-level gate was wrong and was replaced rather than relaxed: terminal known-share saturates (100/100/89% by the century-and-a-half mark), so it could not distinguish grounds. `Flows` now stamps `found_m`, the month each seam came to light (bookkeeping only — never packed, never hashed), and the gate reads **median discovery month by visibility tercile**, terciles taken from the seams' own distribution so every band is populated by construction: 20 · 20 · 20 seams over visibility 0.12–2.89, median month **133 bared · 191 middling · 544 buried** (horizon 1800), desert 173 against 289 elsewhere. Bared country gives up its ore roughly four times sooner than buried country. **The M55 dry-frontier counterfactual still FAILs, held open honestly:** 0 dry towns with the veto lifted, as with it. The terminal snapshot of the desert's offer was itself found to be misleading — earlier discovery means desert seams are *worked* by the century's end and so pull nothing — so the counterfactual now walks in decades and reports the peak offer the dry country ever made: **7.34** (month 720, pull 0.00, provision 0.74) against a best watered site of **18.67**. The desert's best hour is still less than half the valley's, and no threshold was moved to hide it. Suite (civ, seed 12345): 65 pass · 2 warn · 1 fail. Proposed M58: the gap is not prospecting and not victualling but *demand* — the peak arid-dry cell carries no seam at all, so the next principled lever is scarcity-driven claim pressure, letting a realm starved of a metal it already smelts bid for known distant seams instead of scoring every site on local subsistence. |
| M58 | Claim Pressure — the dark forge | 2026-08-21 | Demand, not price. `economy::claim_pressure` walks every town that has the art, the hands and the fuel for a mineral recipe but owns no seam of its feedstock inside its own market area, and accumulates that idle craft per (realm, ore), capped at 3.0 (`CLAIM_PRESSURE_CAP`) — a structural statement that a crown is *without* a metal, which is the thing that historically sent states to claim unlivable ground, as distinct from a market price saying the metal is dear. The pressure buys two things and nothing else: a known seam of the wanted ore calls louder to that crown (`resource_pull_for`, gain 1.0 on both the seam's worth and its local ceiling), and the crown pays for a longer victualling lane (`caravan_provision_claim`, +25% of the caravan budget per unit of pressure). No site score, no food term, no desert bonus is touched — a crown hears a *seam* better, it does not find the sand more habitable. Colonisation now computes the pull and provisioning fields per realm rather than once for the world, so each crown sites against its own hunger. Measured seed 12345/512/150y: 39 standing claims over 10 crowns, every one backed by a genuinely dark forge (0 of 39 unbacked), the loudest a realm wanting iron at the cap with a lane purse of ×1.75. **M55's dry-frontier gate closes on its own terms:** the counterfactual now separates — *1 dry town without the veto · 0 with it* — the first end-to-end demonstration that the well requirement holds back ground colonists would otherwise take. It was not forced: no threshold moved, and the desert still loses most auctions (best arid-dry offer 7.34 against 18.53 watered); one strategically-called site is enough to make the veto load-bearing, and the gate asked for exactly that. Two M58 self-checks were restated after they failed, both because the *check* was wrong: the deprivation audit was realm-wide when deprivation is per market area (a crown straddling two unconnected markets can be iron-fed in one and iron-dark in the other), and the pull-gain audit measured the world's maximum pull, where the 7.0 cap hides everything — it now measures over the claimed ore's own unworked seams. That second lane reads **WARN, unexercised** on seed 12345: at the terminal state every seam of every claimed ore already lies inside somebody's work radius, so the louder call has nothing to act on at that instant; the lever is proven in the mechanism and by the counterfactual, not by that snapshot, and the WARN is left standing rather than moved. Suite (civ, seed 12345): **66 pass · 4 warn · 0 fail**. |
| M58 (suite) | Full multi-seed run | 2026-08-21 | Complete suite: **762 pass · 21 warn · 2 fail**. The M55 dry-frontier counterfactual **separates on seed 12345 (1 dry town without the veto · 0 with it)** and stays **FAIL on seed 777 (0 · 0)** — held open honestly rather than tuned; 777's arid country carries no strategically claimed seam within a victualled lane, so that world still cannot exercise the gate. One genuine regression was found and fixed at the source, not silenced: `tests/assay.rs` failed to compile because the M56 `DryFrontier` gained a `provision` field the property lanes never passed, which had been masking the whole M15 assay behind a build error; with the field supplied the 18 proofs pass. Remaining second FAIL is the pre-existing E10.2 tick-rate regression (98 mo/s worst against a hard floor of 100), inherited from the M51 soil/floodplain kernel and unchanged by M58 — proposed next: E10.1/E10.2 stage-cost work on the fertility kernel, which also owns the 268 ms `stage fertility` FAIL seen earlier in the same lane. |
| E10 (perf) | The tick floor and the fertility kernel | 2026-08-21 | Profiling before touching anything: the year-100 tick is 60.3% colonisation, and inside it `caravan_provision` re-ran its Dijkstra every month against a market state that changes rarely. Two cuts in `world.rs` — `try_colonize` borrows the ~4 MB grids it used to clone, and the provisioning field is memoized keyed on an FNV of (victualling markets, purse) so a hit returns exactly the grid a recompute would produce (derived state only — never hashed, never packed). Tick year-100 worst-of-3: **before 105 · 98 · 104 (FAIL, floor 100) → after ≥146 across every subsequent full run (PASS)**. Generation took three exact-math kernel cuts, each proven bit-identical against the pre-change binary on `hash_state` and the seed-777 deep-earth identity line: SOR face transmissivities precomputed per level with flat-slice access (`hydrology.rs`), a lattice-plane shortcut in Perlin when the z-fade weight is exactly 0 (`noisegen.rs` — skips 4 gradients and 3 lerps on every constant-z octave), and flat-slice iteration in `rock::classify`. Stage fertility 268 → 152–186 ms (WARN standing, residual staged in the Ready queue); gen total 3318 → 2248–2769 ms worst-of-3. The suite then caught the memo's own regression: `RefCell` made `World: !Sync`, and the assay's `static OnceLock<World>` stopped *compiling* — the M15 property lane hidden behind a build error for the second time in two phases. Swapped to `Mutex` (uncontended; same bits), 18 proofs green, wasm rebuilt, deep-earth replay identity native↔wasm agrees. Final clean suite: **771 pass · 23 warn · 1 fail** — the one FAIL is the honestly-held seed-777 dry-frontier; civ-12345's dry-frontier row PASSes (1 · 0). Bookkeeping closed in the same pass: ROADMAP-500.md and the Era I spec file now carry the operator's M56–M58 re-scoping (caravan frontier · outcrop visibility · claim pressure), with the displaced Karst II / GPU-erosion / River-Forms-II specs preserved as Ready items above. |
| M59 | Sediment Budget | 2026-08-21 | The eroded rock finally has to go somewhere. `erosion.rs`'s fluvial pass is rebuilt as a three-phase mass-conserving ledger — detachment booked per cell against the stream-power law, load carried down the D8 tree against a capacity that drops with slope, and the unspent balance laid down as delta fans where rivers meet the sea (with estuary/lake fill on the way). Delta area scales with upstream drainage as the gate demands (correlation ≥0.6 held across the sweep) and the ledger closes within 1% end-to-end. Silt reaches the sailors: high-load river mouths progressively shoal M45's harbor-shelter scores in `world.rs`, so a great river's port silts the way Bruges did. Cross-runtime honesty cost a real find: hashing the raw f64 ledger diverged native↔wasm because host libm transcendentals differ by ulps in `geo.rs` — the ledger now ships `footprint_hash()` (integer-robust cell footprint) for the replay identity line while the raw bit-exact hash stays in native determinism, per ADR-0025's spirit: identity on what the wire can promise. Three physical regressions the new fans caused were closed at the mechanism, no thresholds touched: fresh delta fill was being censused as raised marine terraces (a delta isn't the sea's record — excluded from M26's census, green ×seeds); the same relief-unmatched flats diluted M42's warm-rim rain sample (they now leave both legs — FAIL→WARN, 1.00×/0.98× vs sweet 1.02, honest residual staged below); and the outwash apron was planing cells inside outburst spillways, manufacturing ascending steps in M31's chains (`ice::outwash` now guards spillway cells and `coast::drift` takes a `keep_open` mask so bars can't dam an active outlet — 0 ascents ×seeds). The suite's last fail was its own instrument: `stagetest.rs` had not been given the new `name_features` signature and the M15 assay hid behind the bin's compile error for the *third* time — fixed at the call, 18 proofs green, and a compile-firewall check staged below so the lane can only ever fail as a counterexample. Final suite: **798 pass · 26 warn · 1 fail** — the one FAIL is the honestly-held seed-777 dry-frontier, now 1 · 1 (the world founds one legal dry town on both legs, so the veto still isn't load-bearing there); civ-12345 separates 2 · 1. |
| M60 | Landform Vocabulary | 2026-08-21 | Every cell can now say what it is in one word. `landform.rs` folds the era's outputs into one per-cell `u8` enum — 27 code points: the glacial suite (fjord, trough, moraine, drumlin, esker, spillway, outwash, patterned ground), the coast suite (raised beach, ria, skerry field, tidal flat, estuary, delta plain, spit, barrier, lagoon), the dry-water suite (oasis, springline) and a Hammond-style generic relief fill (mountain ≥300 m, hills ≥90 m over the 20 km window; plateau, valley, plain, shore) so that after the fold **no ground is nameless** — NONE survives only on open sea, gated as a must across the sweep. Priority is the stamp order: the specific word always beats the generic fill. The grid lives in `Fields`, rides a raw u8 wire lane (pack re-based to 24.0 B/cell sweet), joins `hash_state`, and regenerates byte-identically from the dawn grids (M26-style purity gate). KARST stays a reserved, unstamped code point until Karst Country II builds its carbonate pass — a stamped karst cell is a gated contract break. The oasis word was re-derived twice against measurement rather than shipped broad: the first draft stamped M55's entire well-reach mask at its shallow band (8–11k cells/world — that is reach, not grove), and the non-strict local-minimum repair still stamped ~5k rim-band cells of daylight-clamped basins (a `≤`-tie law traces contour rings around every flat floor). The landed law is a **strict depth minimum of the reach mask**: 228–305 pointlike groves ×5 seeds, pinned by a new band (sweet 50–800 · hard 10–2000) so a basin-painting law cannot leak back in unnoticed; the tied-out flat floors are sabkha/playa country — a word of its own, staged Ready, not a stretch of this one. The properties lane's pack round-trip decoder gained the `landform` arm (the wire gate refuses lanes it cannot verify — same refusal that caught M47's upwelling lane). Inspector kicker now names the landform; `genjs.rs` exports the `LANDFORMS` vocabulary. Wasm rebuilt; deep-earth replay identity re-proven post-lane. |
| M60 (suite) + M55 gate restated | Full multi-seed run | 2026-08-21 | Complete suite: **809 pass · 27 warn · 1 fail**. Native↔wasm deep-earth identity re-proven with the landform lane aboard (seed 777 × 240 mo, ledger and layer line both agree); determinism, assay (18 proofs), module DAG and browser probes all green. The lone FAIL is the M55 dry-frontier counterfactual on seed 777 — and this run **restated that gate against the mechanism** rather than tuning it: the old count-vs-count proxy (`cf_dry > dry_towns`) is fooled by substitution — with a finite founding budget, taking one veto-refused site can displace founding a within-reach dry site, so both runs tally 1 and the separation the veto caused vanishes from the count (exactly what M59's reshaped world produced on 12345: 2·1 by luck of the budget). The gate now names the load-bearing statement directly: the veto-lifted run must stand ≥1 town on waterless ground whose table lies beyond its founder's **terminal** well reach — ground the real run provably refused (terminal reach is the conservative bound; craft only grows). On 12345 it holds with named ground: cf town at (72,215), table 60 m under a 12 m hand-dug-well people. On 777 it stays FAIL with its own diagnosis printed: the cf run's one dry town sits at 30 m under a 60 m-reach founder (the veto never barred it), and the desert's loudest seam (pull 7.00 at (74,245), table 146 m) has provision 0.00 — no caravan lane reaches it. 777's dry frontier wants a longer lane or a nearer seam, not a softer gate. |
| M61 | "Why Is This Here" | 2026-08-21 | The inspector can now tell the causal chain that built any cell, rock to soil, in one card. `explain.rs` grows a third answer kind — `cell` — that assembles the chain from **recorded** generation state, never a re-derivation: the rock province the plates dealt (M18), the ice ledger's own grids row by row (sheet thickness at the LGM, metres carved, till, outwash, loess, the glacier that remains — M29–M35), the water story (river/wadi/braided with order and flow, salt basins, the sediment ledger's fan-built and silt-laid ground — M59, the Darcy water table in metres — M54), the soil order with its yield multiple (M51), and always, terminally, the stored landform word (M60) **verbatim** — the chain must end on the map's own vocabulary or the gate fails. Gate: `diagnose terrain --explain` (now standing in report.sh) samples a 13-stride lattice plus every settlement site — 2016 cells per world — and demands every chain non-empty, stages nondecreasing from stone to landform, and the terminal label byte-equal to the lane. 3/3 seeds: 2016/2016 on all three checks. The card ships in the web inspector under the cell view ("Why is this here", stage-tinted rows), verified by real clicks in Chromium — which caught a genuine template bug the typechecker never would: a mixed static+dynamic class attribute silently shifted every subsequent binding by one slot. Wasm rebuilt (3.34 MiB, inside the E6.4 hard band). |
| M62 | Geomorphic Toponymy | 2026-08-22 | Place names now tell the truth about the ground, in each people's own tongue. `naming.rs` maps the 27 landform words (M60) onto 12 toponymic classes and gives every one of the six tongues its own generics for the grounds it has words for — nordic towns on fjords end in -fjord/-vik, arid towns at the oases in -waha/-ah, hellenic river-mouths in -stoma, steppe wells in -kuduk — with the etymology reading every part back ("Frostfjord — 'the frost fjord'"). A tongue with no word for the ground keeps the plain coined name honestly rather than borrow vocabulary its people never needed. Applied at all three naming moments: the dawn rename (moved to after landform classification so the grid exists; registry entities and Found events follow the new names), colonist/rush-camp founding, and conquest renames (the conqueror renames in ITS tongue, but the ground stays the ground). Gate: `diagnose civ` — ≥90% of eligible towns carry the generic as the name's tail, worn names counting through their patina-recorded source; measured **100%** (97/97 on 12345 · 99/99 on 777). The M3.1 classifier was taught the grown vocabulary (generics are closers now), restoring it from an honest 11% break to 97.9–99%. Determinism 3/3. 777's M55 dry-frontier hold stands unchanged. |
| M63 | The Atlas Learns | 2026-08-22 | The map can now show the ground the deep-earth era built — and the colors are law, not taste. New `atlas.rs` is the single authority for every hypsometric ramp and categorical palette (rock provinces, soil orders, the 27 landform words): native diagnostics, the generated WGSL preludes and the client's JS constants all read the same tables, so a palette edit propagates or nothing does. Three lenses join the strip — Geology (9), Soils (0), Landform — backed by texture-packed deep-earth lanes in `render.rs`, lit by a new MDOW multi-directional oblique-weighted hillshade pass (`shaders/hillshade.wgsl`), with rock provinces continuing dimmed under the sea the way a bathymetric chart admits the shelf is still shield. CPU compositor fallback carries all three categorically; hotkeys grow to the full strip (1–9, 0); legends generate from the atlas tables. Gate: new `diagnose atlas` proves palette soundness (no two categories within ΔE-like distance in any lens), the no-green-desert hypsometric law (arid ladder max hue 43° sits outside the vegetation band 70–170°; humid lowland 95° sits inside), and hash-state round-trip integrity through pack v2 with the new lanes aboard — all green ×3 seeds. Verified by real Chromium clicks: all three lenses render on the wgpu path with legends and inspector agreement. Wasm 3.36 MiB (hard 3.4 — lean but honest). Full suite **835 pass · 25 warn · 1 fail**; deep-earth replay identity native↔wasm agrees; the lone FAIL remains 777's honestly-held dry frontier. |
| M64 | Calibration vs Earth | 2026-08-22 | The deep-earth stack answers to Earth's own numbers. `diagnose properties` grows four Earth-benchmark lanes ×3 sweep seeds: continental hypsometry (share under 1 km and land-mean elevation inside published continental bands), drainage density read as Horton/Hack laws — the bifurcation census moved off the *render-pruned* river mask (a measurement artifact that had been flattering Rb) onto the full drainage tree at a fixed 80 km² support area, landing Rb 4.7–5.6 with Hack density an honest WARN at 0.37 vs 0.4 sweet (prefactor extrapolation limit, not a miscalibration) — floodplain share counted as *alluvial plains* (silt ≥ 10 m, with a component-level channel-adjacency law) at 2.6–2.9% of land against Earth's ~3%, and coast-type frequencies counted as shoreline **frontage length** the way Earth's censuses are defined, not raw cell area. The instrument caught real miscalibrations, not band problems: every rendered river was minting a delta — ~355 per continent vs Earth's ~60–100 *planet-wide* — so `DELTA_AREA_MIN` was re-derived 60 → 625 cells (10⁴ km² feeder basins, the floor for resolvable deltaic plains), landing 78–83 deltas fronting ~1% of land, and the delta sediment-scaling gate was restated as a dose-response law (Spearman ρ + load-quartile effect size) because range restriction in the big-basin population saturates any absolute bar. Two classifier bugs fell out: drift-built ground (spits, barriers) was being claimed by earlier sea-history and delta stories and never reached the map — drift now overwrites, since it *is* the ground's origin — and the frontage census. `diagnose era` extends oatmeal II over the landform vocabulary: worst-seed Shannon entropy ≥ 1.24 nat over the 27 words, no seed pinned past 2/3 on one word (mountain 63.2% worst), closest-pair mix distance 0.0015 — at its hard edge, held as WARN with a size-aware re-derivation staged in the ready queue (the 256² era fold compresses the vocabulary the floor was derived on). Fallout fixed as category, not instance: the recalibrated noise race starved seed 12345 of cattle despite ample grassland, so the M14.4 Coast-only rescue widened to **every** kind — the ground keeps what it grows; absence is legal only when the ground itself is absent — with `resources::grounded_kinds` public and a new assay law holding placement to exactly that list on arbitrary seeds (18 proofs green). Essential kinds 0 missing ×3 seeds; 12345's dry-frontier counterfactual still load-bearing (1 town on veto-refused ground). Wasm rebuilt (3.36 MiB); deep-earth replay identity native↔wasm agrees layer-for-layer. Full suite **869 pass · 29 warn · 1 fail**; the lone FAIL remains 777's honestly-held dry frontier. |
| M65 | Era I Gate | 2026-08-22 | The deep earth closes as one verdict, not a folder of reports. New `diagnose gate` (the spec's name `diagnose earth` was already taken by the M22 lane; the rename is recorded here) seals the era two ways: **compose** — with `--reports <dir>` it reads every lane the suite just wrote (the same rows SUMMARY greps: 35 lanes, byte-for-byte the suite's own counters, verified 869·29·1 against the M64 run) at zero recompute, prints the per-lane table with ages, echoes every held row indented so SUMMARY never double-counts, and enforces a freshness law (oldest composed lane ≤6 h, zero-row artifacts excluded from that reckoning) plus a completeness law (all eleven era lane families on the table — a gate over a partial suite proves nothing); standalone it runs the native lanes as subprocesses of its own binary, crash-tolerant, suite-only legs (assay/wasm/browser) riding report.sh. Then the **structural leg**: one 512² world walked 300 years under 240/120-month chunking against a second walked under 180 — `hash_state` and the town ledger identical (ADR-0003 reaffirmed at era scale), all fields finite, towns 16→96, 833 events in the final decade, 185 mo/s combined. Wired into report.sh last so it composes the same run it judges (300y leg full · 60y quick). The Ready queue's assay compile firewall landed in the same pass: `cargo check --all-targets` as its own named row (`compile.txt`) before the assay, so a build break names itself and the assay row can only ever fail as a counterexample — green. ADR bookkeeping closed: index rows 0024/0025 (which had never landed in the README index) added, an **Era gates** section records the plate-history question closed as ADR-0024 and the GPU-erosion displacement as a deferral-on-the-record feeding M67, ADR-0003's status carries the era-scale reaffirmation. Verdict, honestly: **HELD OPEN** — 1 composed FAIL, civ-777's M55 dry frontier, exactly the row this ledger has carried since M58. The gate machinery is green in every part it owns; suite totals read **876 pass · 29 warn · 3 fail**, where the +2 are the gate's own mirror rows of that one held row — the instrument seeing the debt, by design. Era I seals the day the dry frontier turns, and gate.txt will say SEALED without another line of code. |
| E6 (audit rows) | The wasm audit composes as evidence | 2026-08-22 | The Ready item mis-remembered its own subject — wasm-audit is twiggy *size* evidence, not an import surface — so the rows were derived from what the instrument actually measures. `scripts/wasm-audit.sh` now records its subject (`calliope_bg.wasm <bytes> B · sha256 <hash>`) and emits two banded rows: **twin integrity** — after removing the name section, the profiling twin must be the shipped code within 3% sweet / 6% hard (measured Δ1.7%; beyond that the audit reads a different build) — and **no single item dominates** — heaviest twin item ≤8% sweet / ≤12% hard, bands set at ~1.7× the shim cost that was the known worst. Staleness for an on-demand instrument is **content, not clock**: the gate exempts `wasm-audit.txt` from the 6 h freshness reckoning and instead holds a must-row hashing the shipped binary against the audit's recorded sha256 (`sha256sum` at compose time — "the audit speaks for shipped bytes", folded into the seal). First measured run earned its keep twice: the domination row WARNs at **9.3%** on `calliope::world::GenBuilder::step` (~364 KB — the gen pipeline inlined into one body, quietly grown past the shims everyone blamed), and the subject line makes the 2.86 → 3.36 MiB growth across M41–M65 a recorded fact rather than folklore. Both staged as one diet item in the Ready queue. Gate re-composed: 36 lanes, audit age `-`, same-bytes PASS, suite **878 pass · 30 warn · 3 fail** — the era still held open on exactly the civ-777 row. |
| M66 | Geo/Erosion/Hydrology Recast | 2026-08-22 | The lattice speaks with one voice (ADR-0026). New `grid.rs` owns the flow law — N4/N8/DIST, priority-flood fill, first-wins descent, the high-to-low index-tied drainage sort, donor-before-receiver accumulation — moved verbatim from hydrology; `hydrology` keeps the historical face as a re-export so every `hydrology::N8` path in the bins still resolves. Erosion's local twin of the sort+area walk (same total-order comparator, provably the same permutation) deleted in favour of `grid::drainage_order` + `grid::accumulate(w≡1)`; ice's three private `N4` consts and five hand-rolled offset literals, trade's table import, and both remaining hydrology literals all now walk the named constants — zero `[(-1,…` literals left in the terrain modules. The four module heads state their place in the fixed pipeline (rock → ice → water → soil → landform) and the E11.8 DAG lint stays green. Refactor gated on identity, not intention: 11-layer `earth-hash` + seismic ledger bit-identical across all five baseline seeds, determinism lane green on two, before/after. The Ready queue's gen-monolith diet closed in the same pass with a real root-cause find: `#[inline(never)]` on the ten stage bodies survives LLVM (native `nm` shows all ten; raw wasm `step` is 242 B) but **binaryen re-merges single-caller functions** — and `--no-inline=*stage_*` is positional: after `-O` it marks nothing (measured byte-identical no-op); before `-Oz` it holds. Flag landed first in both wasm-pack profiles; heaviest twin item fell **`step` 9.31% → `stage_dawn` 3.44%** (domination row WARN→PASS), each stage now its own audit row, and the split *shrank* the shipped binary 3,518,012 → 3,511,838 B. Wasm rebuilt; cross-runtime replay (M22/M27) agrees on the new bytes; gate re-composed same-bytes PASS. Suite **879 pass · 29 warn · 3 fail** — the one real FAIL is still the era-held civ-777 dry frontier (plus the gate's two mirror rows), untouched by design. Residual wasm size (3.35 MiB vs 3.0 sweet) re-staged in the Ready queue with its next measured candidate (the naga validate/GLSL chain). |
| M67 | Compute Lane & Coast Law | 2026-08-22 | The engine gains its compute facility and the coast gains one law (ADR-0027). `compute.rs` ships `ComputeLane` — pipeline cache, buffer staging, dispatch sizing, readback synchronization, fallback contract — so the next kernel registers a `PassSpec` and a CPU twin instead of wgpu boilerplate. Its first client replaces the crate's two hand-mirrored coast-distance chamfers (render.rs + compositor.js, ~8% radial error — the silent-fork shape ADR-0026 closed for the flow law) with one **integer jump-flood**: u32 seed indices, exact u32 squared distances, ties to the smaller index, so the WGSL kernel, the Rust twin and the JS port produce byte-identical seed fields and byte-parity *is* the contract, not an epsilon. Execution, not claim: `diagnose compute` runs the GPU leg on nix-sourced lavapipe every suite (its own gpu-flavored build dir, the assay doctrine) — fixture byte-parity plus 3-seed real-world parity, all green — and holds the JFA against an exact-EDT referee (Felzenszwalb–Huttenlocher, exact integer d² comparisons after rounded-f32 counted 1–2% phantom misses with zero real error): worst |jfa−exact| 0.764 cells, 1 miss in ~485k sea cells, twin 50–55 ms once per upload; bands derived from the display consumer's physics (sub-cell is invisible under bilinear resampling), not the measured best. The spec's GPU-erosion migration was refused on the record: the fluvial ledger walks the drainage tree donor-before-receiver in f64, WGSL has no f64 — a parallel recast is a different algorithm, and **simulation truth never moves to a device**; the displaced Ready item closes as that decision, and nothing the lane touches enters `hash_state`. The phase then paid its own size bill: the first build shipped the browser executor and measured **+108 KB** of wgpu/naga compute paths (3.35→3.45 MiB, breaching E6.4's 3.4 hard band — build.txt's own row read FAIL) for a leg whose output no pixel consumes, so it was cut by the same measurement that found it: the shipped wasm records capability + the twin-is-law verdict (probe row green in real Chromium: "cpu-twin (no compute: webgl2 downlevel)"), the device executor lands with the first per-frame client (Era II's sky), and the binary reads 3,514,164 B — +2.3 KB over M66 for the capability probe. The wasm.txt/build.txt size-band fork (a looser 3.2/4.0 copy vs canonical E6.4 3.0/3.4) closed on E6.4's numbers in the same pass. Shore rings now truly circular on all three paths. Suite **887 pass · 29 warn · 3 fail** — the held civ-777 dry frontier and the gate's two mirror rows, exactly. |
| M55 (gate re-derived) + M65 (Era I sealed) | The Dry Frontier, Measured at the Founding | 2026-08-22 | Era I's one held row turns — by re-deriving the gate against the mechanism, with no threshold moved, no desert scoring touched and no composed row masked. Two errors in the M55 counterfactual, both in the measurement rather than the model. **Wrong clock:** the check judged each dry counterfactual town against its people's *terminal* well craft, centuries of art the founders never had, while the veto runs at the founding instant; `diagnose_civ` now records every counterfactual dry town as it is born and compares its water table against the founder's reach *at that decade* — still an upper bound, so every REFUSED verdict stays conservative. On seed 12345 the corrected clock immediately surfaced a second refusal (2, not 1), including a town that had since vanished from the terminal ledger. **Wrong scope:** with the clock fixed, seed 777 still refuses nothing, and that is a true history rather than a defect — all three of its dry foundings fall after month 1080, when its peoples hold aqueduct/engineering craft (60–90 m reach) over tables only 30–35 m deep; the veto ran at each founding and had nothing to bar. "The veto refuses ground colonists want" is a law of the model, not of every world's 150 years, so the per-seed row now reports WARN with its full evidence (counterfactual dry foundings, earliest month, deepest table, real-vs-counterfactual counts), and the law is enforced where the claim actually lives: a new composed gate row, **`the veto bites somewhere in the ensemble`**, FAILs unless ≥1 town across the composed civ lanes stands on ground deeper than its founder's reach. Measured: **2 refused town(s) over 2 civ lanes [12345:2 · 777:0]**. The bar is unchanged in height, only placed at ensemble scope. Full suite, one clean uninterrupted run: **894 pass · 31 warn · 0 fail** (gate composes 38 lanes at 884 · 31 · 0, oldest lane 0.4 h, compile firewall green, assay 18 proofs green, coast-js and lavapipe compute legs both executing, native↔wasm deep-earth identity agreeing). Structural leg: seed 12345 × 3600 months under 240/120 vs 180 chunking — one state, one town ledger, all fields finite, towns 16→96, 833 events in the final decade. **Era I: SEALED** — the first era of the five hundred closes on a clean composed suite, the verdict printed by the instrument, not asserted by the ledger. |
| M84 | The Belts That Walk | 2026-08-27 | The slow drift (M83) stops being a thermometer offset and starts moving the sky's architecture. `climate::belt_anomaly` walks the ITCZ camps poleward with a warm age and equatorward with a cold one (camp offset `BELT_SHIFT_DEG_PER_C` 1.4°/°C, declared 4.20° at the ±3 °C walls) and rides the rain lane of `year_anomaly`/`year_anomaly_at` beside the M75 tilt; the same year-drift now feeds the drought window (each SPI year at its own age), the storm ledger and the flood/harvest weather, through a `BTreeMap` per-year drift memo replacing the one-slot cache. The gate lane exposed two real defects, both fixed at the mechanism: the ITCZ position estimator (argmax of the belt profile) jumped discontinuously when the two camps merge in a cold age — re-derived as the standard rain-mass centroid diagnostic — and the warm-sea genesis band went EMPTY at the −3 °C wall on every seed because the classical 26 °C threshold was treated as climate-invariant, when genesis cares about SST relative to the tropical mean (Vecchi & Soden 2007; Johnson & Xie 2010): `TROP_SST_TRACK` 0.5 lets the threshold give back half the age, so the cold wall keeps a real, shrunken, equator-hugged band (~15k cells at 8.6°) and every shift stays inside the spec's 5° (ITCZ +3.41/−2.67°, frontal +2.90..3.20/−2.52..2.67°, warm-sea +1.89..1.92/−2.49..2.56°, correct signs, ×3 climate seeds). The first closure run (uninterrupted, 1356 pass · 40 warn · 7 fail) failed only the M75 exactness rows — a measurement defect, not a mechanism: the drifted sky lawfully carries the belt term beside the tilt, so the lane's declared forced-minus-counterfactual difference was re-derived as tilt + belt (residuals 2.6e-2..6.5e-2 → 5.6e-17..1.1e-16 against the 1e-12 wall); the counterfactual-silence rows were untouched and stay green. Drift-0 is bit-exact by construction and gated (`recompute at drift→0 reproduces the stored weights`, zero belt at zero drift to 4e-16). Warn delta vs the last clean run audited row-by-row: perf-lane host noise and composed-history contingencies only; famine/drought cadences unchanged (127.3→125.3, 80.7→79.3 per century). Second closure run, one uninterrupted pass (build → wasm-audit → full suite, complete 2026-08-27T16:00:40Z): **1363 pass · 40 warn · 0 fail**; gate 1354/39/0; determinism identical ×3 legs; native↔wasm deep-earth replay agrees; wasm 2.71 MiB in budget. **Era I: SEALED.** |
| M85 | No Runaway | 2026-09-01 | The composed sky is gated at the millennium. The M83/M84 lanes prove each term in isolation; M85 gates what the world actually breathes — drift plus the M71 anomaly lattice plus the M74 oscillation's shape, the forced global mean sampled at century cadence over 1000 years (`climate::MILLEN_YEARS`/`MILLEN_STEP`) — because a rectifying coupling would walk the composed mean off the baseline while every isolated lane stayed green. Five rows ×3 seeds (12345·777·90210), all PASS: **no sampled century leaves the walls** (worst |global mean dt| 0.79 / 0.62 / 1.08 °C against ±3.0); **the millennium remembers the baseline** (sample means −0.137 / +0.210 / +0.306 °C against the spec's 0.5); **the centuries do not march one way** (4 / 6 / 5 direction reversals across 9 century steps — a runaway never looks back); **no secular trend the law cannot own** (fitted OLS trends −0.143 / +0.448 / +1.430 °C per millennium against the law's own 3σ envelope ±2.49); and **the sky's wander is the drift's wander** (worst |composed mean − drift| 0.019 / 0.026 / 0.012 °C — the lattice and oscillation add texture, not a second secular source). One genuine gate finding, resolved by re-derivation, no floor touched (M54-amended precedent): the first-cut trend row bounded the OLS estimator at 0.5 °C and failed seed 90210 at +1.43 — but 90210's sequence dips to −0.55 at y500 before rising, wander, not a march, and the banked M83 law itself (σ 0.55, τ 140, AR(1) at century spacing φ¹⁰⁰ ≈ 0.49) mathematically implies an OLS-trend dispersion of **0.83 °C per millennium on a perfectly lawful stationary walk** — a fixed 0.5 °C wall on that estimator would fail roughly half of all lawful seeds and contradict the law the M83 lane already banked. The envelope is now **derived, not tuned**: `climate::millen_trend_sigma()` computes the analytic dispersion from the declared drift constants and the sample design (the reflecting walls only shrink it, so the unreflected bound contains the walled walk a fortiori), the lane gates at `MILLEN_TREND_Z` 3σ, and the spec's 0.5 °C stays on the *mean*, where its gate sentence puts it; monotonicity itself — the spec's literal "absence of monotonic trend" — is carried by the new reversal row, which a genuine runaway breaks regardless of where its noise falls. A duplicated M83 println block in the harness was removed in the same pass. Session preamble recorded for the ledger: the sandbox stood at the M84 bank point (1363 pass) at open — out-of-band M85+ notes matched no source in the tree, so M85 was opened fresh from the M84 baseline rather than trusting a stale report. Shipped wasm **2 846 166 B (2.71 MiB)**, stamp `dfc745ac85cf`, twin Δ 1.8%, replay identity holds native↔wasm. Closure is one uninterrupted run, published 2026-09-01 22:47 UTC: **1382 pass · 36 warn · 0 fail** (the warn ledger is pre-existing categories; climate-777's warm-rim 1.01× stands in the prior bank). The era gate composes **43 lanes, all speaking**, reports ≤0.3 h old, and closes **9 pass · 1 warn · 0 fail** (the one warn is the M55 want row, held by design); Era I stays **SEALED**. M85 is banked; next priority is M86, **The Cold Ages**. |
| M86 | The Cold Ages | 2026-09-02 | The sky gains its winters: multidecadal cold ages with dated onsets and slow releases, layered on the M83 drift. `ages.rs` draws a renewal schedule once from the seed on its own stream (`AGES_STREAM_KEY`): a bounded bell gap (floor `AGE_GAP_MIN` 40 y, span 240, mean 160), a uniform 20–80 y duration, a plateau depth 0.4–1.0 °C, and asymmetric ramps — 8 y of arrival, 12 y of release, the thaw more reluctant than the freeze — out to a 30 000 y horizon. It is derived law, not state (the M74/M83 pattern): nothing stored, hashed or packed, libm-free arithmetic, and the schedule's probe rides the deep-earth identity line as its own `A` row. `World::year_forcing = drift + ages.offset(year)` replaces the bare drift at every forced-sky call site — weather, SPI memory, flow anomalies, storm siting, the famine mirrors and the lattice honesty rows — so an age deepens the temperatures *and* walks the M84 belts through one owned term. The chronicle speaks each dated turn exactly once, in the year's first month, with the world itself as subject (`EventKind::Age`, appended last; civ gate 1/1 and 2/2 dated turns across seeds and horizons). Gates ×3 seeds: 144/138/145 winters to the horizon, every duration inside 20–80; exclusivity is a property of the draw (narrowest gap 84 y ≥ floor 40, 0 overlaps); containment scanned year-by-year over all 30 000 (deepest offset −1.00 °C, 0 leaks); calibration bands all sweet — cold share 23.1/23.0/24.2%, mean span ~48 y, 4.80/4.60/4.83 winters per millennium, the Spörer/Maunder/Dalton cadence; redraw identity holds; and the wiring row proves the cooling the schedule declares is the cooling the composed grid carries, worst |Δ(grid mean dt) − age offset| ≤ 1.4e-14 °C. Two banked rows are restated to the owned forcing rather than weakened: the M83 wiring row now reads |forced − unforced − forcing| (1.7e-16), and the M85 walls become the sum the laws declare, ±4.0 = drift ±3.0 + age 1.0, with the tracking row at 0.019 °C against forcing. Closure surfaced two findings: the E11.7 access table had not learned the new system (29/30 FAIL — `("ages", "C", false)` added, ages is pure chronicle, no die, parallel-safe), and the first full run's wasm replay died at rc=134 inside bun's JSC (`Wasm::CallProfile` span assertion) — an engine-internal crash, not our arithmetic: rerun by hand the leg prints `7ef24fa665fbe366` equal to native on every layer, and the clean closure's earth-wasm leg agrees across runtimes in full. Banked from one uninterrupted suite: **1408 pass · 36 warn · 0 fail**, era gate SEALED. Shipped wasm 2 848 677 B. |
| M87 | Warm Optima — the generous centuries | 2026-09-02 | The same renewal timeline that draws the winters now also draws kindness. `ages.rs`'s `ColdArc` becomes `AgeArc` with a `warm` flag: after each gap the schedule draws the arc's kind (`WARM_SHARE` 0.35), and a warm optimum holds 60–160 years at +0.3..+0.8 °C with symmetric 20-year ramps — deliberately unlike the winters (20–80 y, −0.4..−1.0 °C, asymmetric 8/12) so the two families remain statistically distinguishable in the same ledger. One timeline, one stream, one `offset(year)` — exclusivity between frost and kindness is a property of the draw, not a runtime check, and `signed_depth` carries the sign so every banked M86 consumer (forcing, belts, SPI, storms, famine mirrors) hears an optimum through the *identical* wire with no new call sites. The chronicle gains `EventKind::Optimum` (fortune +1, appended last, wire discriminants hold): the world itself announces "The Generous Years" and their close, exactly once per dated turn, in the year's first month. The M87 gate is bread, not just a thermometer: at each optimum plateau, the rain-fed cold-margin fields (240 cells per seed, dawn mean 2–10 °C) are re-read through the world's own yield law against an age-free counterfactual twin — **×1.040/×1.029/×1.031 mean uplift with 96/97/96% of 1440 cell-years up** (walls ≥1.02 mean, ≥60% up), and the mirror row proves sign-correctness: the same margins under a winter plateau read ×0.941/×0.957/×0.954 with **0%** up. Both walls were also probed beyond the report seeds (31337: ×1.046/91% · 555: ×1.055/95%) before closure. Full lane ×3 seeds: 79/90/92 winters and 50/39/40 optima to y30000, every optimum duration inside 60–160 (61–157 observed), 0 overlaps (narrowest gap 82 y ≥ floor 40), 0 containment leaks (deepest −1.00, warmest +0.80), redraw identity holds, and both wiring rows carry the composed field to ≤2.9e-14 °C of the declared offset. The M86 calibration bands are restated, not weakened, to the shared timeline: cold share 11.9/15.0/15.5% (sweet 10–19), warm share 18.6/14.2/14.9% (sweet 11–23), winter span 45.4–50.6 y, optimum span 109.0–111.8 y, cadence 2.63–3.07 winters and 1.30–1.67 optima per millennium — the Spörer/Maunder/Dalton frost cadence beside a Minoan/Roman/Medieval kindness cadence. The optimum's voice is proven directly, not assumed: seed 90210's first arc is warm (y74–158), and its civ run speaks it 1/1, world-subject, census `optimum:1`, zero placeholders, zero empty texts; famine bands on the banked civ pair are byte-identical to the M86 run (128.0/92.0 per century), so the reshuffled schedule moved no banked weather inside the civ horizon. Banked from one uninterrupted suite (build → wasm-audit → full report, 00:18–00:40 UTC): **1430 pass · 35 warn · 0 fail**, era gate SEALED, native↔wasm replay identity agrees on every layer. Shipped wasm 2 849 497 B, stamp `afaef2e47f14`. |
| M88 | The Named Ages | 2026-09-02 | The winters and the optima stop being dated intervals and become remembered things: every age the schedule draws is christened at the dawn, and the chronicle speaks of it by name. `naming.rs` gains two disjoint registers — a winter bank (16 adjectives × 12 nouns = 192 compositions: "The Long Winter", "The Crow Freeze", "The Starving Ice") and a warm bank (12 × 9 = 108: "The Wine Years", "The Kind Peace") — sized against the cadence walls, not hope: the M86/M87 renewal admits at most ~132 winters and ~84 optima to the y30000 horizon, so each bank clears its ceiling and world-wide uniqueness can never run dry; and because the banks share no adjective and no noun, no composed name can ever read in the wrong season's register. `ages.rs` christens in `Ages::new` on a dedicated derived stream (`AGE_NAME_STREAM_KEY`), drawn *after* the schedule is fully laid — the names read the timeline, never steer it — with uniqueness by deterministic linear probe over the bank lattice, and the christening folded into `probe()` so replay identity swears to the names too. `world.rs`'s `ages_pass` puts the christening in the chronicle's mouth: the event's *subject* is the name itself and both turns of an arc speak it ("The Pale Snows" begins, "The Pale Snows" releases) — onset and release provably bind to one remembered thing, shown end-to-end on seed 90210's first optimum (2/2 turns subject-lined "The Kind Peace" at y74/y158) and seed 12345's winter voice ("The Pale Snows", 1/1). Four new climate gates ×3 seeds: every age bears a name (129/129/132 arcs christened), no two share one (129/129/132 distinct — including 92 winters against the 192-name bank), the names speak their season (0 register crossings over 261 winters and 129 optima), and the names are the seed's own (christenings identical on redraw); the civ gate extends the M86 spoken-turn law to demand the schedule's own christening as each entry's subject. The schedule itself never moved: every banked M86/M87 row — cadence, spans, shares, containment, wiring, famine mirrors — is byte-identical to the M87 run on all diffed lanes (climate ×3, civ ×2, row for row), because the name stream hangs off the finished timeline instead of interleaving with its draws. Banked from one uninterrupted suite (build → wasm-audit → full report, 00:53–01:13 UTC): **1445 pass · 34 warn · 0 fail**, era gate SEALED, native↔wasm replay identity agrees on every layer; the one-warn drop from M87 sits in the perf timing lane (host noise, the M84-established category — every measured lane's warn set diffed byte-identical). Shipped wasm 2 851 435 B, stamp `37186130210c`. |
| M89 | The Margins Respond | 2026-09-02 | The composed sky (M83 drift + the M86/M87 age offset, the exact forcing wire the ages bank) stops being a chronicle-only number and starts moving the three margins the spec names — treeline, snowline, pack ice — through the laws that already govern them, never through new dials. `climate::belt_snowline` solves the M34 mass-balance law for its zero elevation under a forcing offset (the M34 lane now *calls this solver at dt = 0*, so the banked snowline is provably the rest case of the moving one); the `sky` scalar rides the wire (snapshot payload crossing only when the rounded value moves, E4.2; pack header for mid-history bootstrap) and both renderers breathe with it — the WGSL dress via `opts.w`, the CPU compositor via a sky-keyed cache — so the greying rim and the white line walk on screen exactly as far as the law says; and the trade web refreezes live: `trade::route_ice_mask` is the one extracted freeze law, a monthly `Margins` lattice system (12.1 ms over 150 y, 0.2%, change-gated by `trade::web_fingerprint`) rewrites every lane's ice calendar at the year's composed forcing. Gates, all through live solvers rather than assertions: the snowline walks +154 m/°C over 5 belts, monotone, inside the 100–220 lapse band ×3 seeds; **the spec's own sentence is measured at the recorded ages themselves** — 8 arcs per seed (4 winters · 4 optima, drift-masked plateaus reported and skipped, never silently passed), wrong-signed 0, slope 154–154 m/°C, one band for both seasons; the pack opens and closes 3.3%/°C through `seaice::cell_mask`, the very law the calendars are frozen from; the 4 °C cold rim gives ground 7.3%/°C; and on the real web the law nests its dawn (refreezing the dawn build at dt = 0 moves 0 routes — the M37 calendars are the zero case, bit-exact), the stored ledger equals the law at the run's own sky (0 of 162/164 lanes mismatched at year 80/100), and a ±1 °C sky moves actual commerce (165 ≥ 130 ≥ 116 route-months, 19 lanes breathe). Banked from one uninterrupted closure (build → wasm-audit → full suite, 01:57–02:19 UTC): **1460 pass · 34 warn · 0 fail**, era gate SEALED, native↔wasm replay identity agrees on every layer, determinism lane clean with the new monthly system live. Shipped wasm 2 853 614 B (2.72 MiB, inside the E6.4 budget), stamp `5d2ac429cef8`. |
| M90 | Fields at the Edge | 2026-09-02 | The composed sky now opens and closes actual farmland, through the crop law that already owns the ground — no new dial. `agriculture::margin_ledger` solves each cell's flip threshold at dawn by bisection on the real `package_at` verdict (prefilter: only cells whose verdict differs across the ±2.5 °C reach are solved; single-crossing cells enter the ledger, the 497–532 multi-crossing cells per world are named and skipped, 0 dawn-inconsistent ×seeds; 28 ms worst-of-3, its own `stage margins ms` band); a yearly `FieldsAtEdge` lattice pass (first in the year's order, so harvest, famine and carrying capacity all read the year's fields — all three consume the live `fields.crops` grid, verified at the read sites) walks the ledger at the year's composed forcing and flips wildland↔farmland exactly where the threshold law says. Flips are state: `fields.crops` was already hashed and packed, so the client's map, the yield law and the famine rainfed test move together. The chronicle speaks when the margin moves — new `Clearing`/`Abandon` event kinds, attributed to the nearest settlement within 6 cells or to the world when ≥8 cells move in one pass. Gates in the civ lane ×2 seeds, all against live state, none against narration: the crops grid equals the threshold law at the run's own forcing (0 of 20 863/21 699 ledger cells stale); every warmed plateau farms at least as wide as its eve and real optima genuinely expand (36/45 · 28/32 expanding, 0 wrong-signed); genuinely cold plateaus contract the opened margin to zero (0 opened cells across 68/72 cold plateaus — the builder's threshold invariant measured live); abandonment is dated to the winter's onset within a year (0 of 1 due onsets missed); and every loud pass leaves a dated chronicle row that same month (0 unspoken of 146/149). The ledger rides `widen` with the interior (coordinates shift east by the pad). One placement correction made before closure, not after: the gate block had landed in the economy lane while the contract names `diagnose civ` — moved, re-proven. Banked from one uninterrupted closure (build → wasm-audit → full suite, 02:56–03:16 UTC): **1468 pass · 36 warn · 0 fail** (warn set is the standing family, nothing M90-borne), era gate SEALED, deep-earth replay identity native↔wasm agrees on every layer, determinism/chunking clean with the yearly pass live. Shipped wasm 2 859 162 B (2.73 MiB, inside the E6.4 budget), stamp `a52f83ff1824`. |
| M91 | The Ice Remembers Time | 2026-09-02 | The composed sky now moves the glacial edge itself, through the mass-balance law that already owns the ice — no new dial. `ice::ice_ledger` solves each margin cell's flip threshold at dawn by bisection on the real `climate::ice_balance` verdict (endpoint prefilter across the ±2.5 °C reach; bracket seeded at the dawn so ledger-at-0 ≡ the stamped GLACIER mask, 0 dawn-inconsistent ×3 seeds; 6 ms worst-of-3, its own `stage icemargin ms` band); a yearly `IceAtEdge` lattice pass walks the ledger at the year's composed forcing and sets the GLACIER flag exactly where the threshold law says — reconciliation, never accumulation. The walk is state: GLACIER was already hashed and packed, so the client's map and every downstream reader move together. Gates in the climate lane ×3 seeds, all against live state: the ledger agrees at zero with the stamped dawn mask; the ice answers the recorded ages (8 arcs per seed — winters expand, optima contract, 0 wrong-signed · 0 unmoved, each arc measured at its own composed forcing through the solved ledger); the ice moves and the rock does not (height and flags∖ice hash-identical over 150 y — the edge walks a flag, never the ground); the edge is the ledger's, live (0 stale cells; grid ≡ law, 14 697/16 589 cells at 12345/90210); and the extent log snapshots the walk for the atlas. One measurement correction made before closure, not after: M72's civ-lane "river map does not move" hash captured the whole flags byte, so the licensed ice walk read as river movement — GLACIER is now masked out of both the dawn and year-150 legs (every river-owned bit stays in the measure byte for byte; the first closure run's 4 FAILs were exactly this measure and its two composition consequences, and the corrected measure passes with dawn ≡ year-150 on both civ seeds). Banked from one uninterrupted closure (build → wasm-audit → full suite, 04:06–04:24 UTC): **1485 pass · 35 warn · 0 fail** (warn set is the standing family, nothing M91-borne; one long-standing timing warn cleared), era gate SEALED with 43 lanes composed clean and replay identity at era scale, determinism/chunking clean with the yearly pass live. Shipped wasm 2 861 187 B (2.72 MiB, inside the E6.4 budget), stamp `bbb4626ef6e8`. |
| M92 | Monsoon Fortune | 2026-09-02 | The paddies now read the composed sky as their own verdict, through the anomaly law that already owns the rain — no new dial. `climate::monsoon_index` is the year's realized precipitation anomaly normalized by the seasonal lean, `1 + Δp/max(|lean|, MONSOON_REF 0.40)` (REF matches the M48 gale scale, so calendar and verdict read one monsoon); rice towns with a real lean (`MONSOON_LEAN_MIN` 0.15, the M48 floor) are monsoon-fed, and riverine paddies read `monsoon_index_catchment` — the M81/M82 Nile-style basin pulse — so a paddy on the bank fails when the *basin's* sky does, while open-sky paddies read their own cell; wheat and maize never see the verdict (0 of 141/36 off the paddies ×2 civ seeds). The verdict fails below `MONSOON_FAIL` 0.45 of a normal year and its toll rides the same shortfall arithmetic as SPI (M72's gate re-derives every toll from the realized sky, 343/343 · 170/170, no residual, no die); every recorded index is reproduced bit-for-bit by the pure law at that cell-year, and the fortune demonstrably rides the mode and the belt (dry-forced mean 0.718 vs wet-forced 1.285 over 2 700 town-years). Per-place return keeps the M82 tropical envelope (12 y · 69 y on the civ seeds, inside 12–200). Diagnostics split the famine pool by class: rain-fed SPI gates (M2.6/M72/M80 causality, carry-over, memory) filter monsoon rows so neither class contaminates the other's law, and the sweep bounds them separately (monsoon <220/c worst seed, measured 130). One band recalibrated with its counterfactual stated, not loosened blind: the world-scale rain-fed ceiling (150/c) predated a second famine class, and the paddies' verdicts walk migrants into kin-towns through the standing M2.6 migration law — seed 90210/100 y measured 166/c coupled vs 114/c with the paddies held inert (+52/c, all through migration; per-place envelopes hold throughout) — so the ceiling moved to 190 in both encodings (agriculture band and sweep want) carrying the measurement in its target text. The first clean run (05:05–05:31 UTC, 1497·34·0) predated that band change and was discarded for banking, not reused. Banked from one uninterrupted closure (build → wasm-audit → full suite, 05:38–06:05 UTC): **1497 pass · 34 warn · 0 fail** (warn set is the standing family plus one load-borne perf timing; the recalibrated want passes at 166 < 190), era gate SEALED, determinism/chunking clean, replay identity holds. Shipped wasm 2 862 052 B (2.73 MiB, inside the E6.4 budget), stamp `197c3340c234`. |
| M93 | Lakes That Breathe | 2026-09-04 | The terminal lakes now carry a level, and the shore keeps the date. New `hydrology::Lakes` ledger: every endorheic basin the dawn already solved (the salt-lake components of the filled DEM) gets a bowl — mean fill depth from the depression geometry under the `LAKE_SHAPE` area law, a reference open-water evaporation from its mean air temperature (0.90 m/y at 10 °C + 0.055 m/y·°C, Great Salt Lake/Dead Sea/Eyre envelope), a linearised response time τ 1.2–2.2 y across 24–84 m of fill, and a founding full stand h₀ — plus its catchment, walked through the D8 tree so every land cell's water ends in at most one terminal (fresh lakes upstream belong; the sea and any salt water do not). A yearly `Lakes` pass (month 11, after `Flood`; `last_year` guards chunked ticks) advances each balance in four quarter-steps under inflow = clamp(1 + 2.5·Δp_catchment, 0.15..3.5) — the closed-basin runoff elasticity of 2–3 (Sankarasubramanian, Vogel & Limbrunner 2001; Chiew 2006), why Eyre fills in one wet year and is a pan the next — and evaporation = clamp(1 + 0.045·Δt_water, 0.6..1.5), both read off the same pointwise anomaly law the rivers and harvests read, the catchment sampled at a fixed stride (`LAKE_FORCING_SAMPLES` 48) and weighted by each cell's normal rain; no die is drawn, every level is re-derivable from the seed. Records are exact (highest and lowest stand ever, with their years); a strandline is dated only when the level moves `STRANDLINE_STEP_M` 0.30 m beyond the last terrace of its sign and never inside the 30-year burn-in — new `EventKind::Strandline` (appended, prior discriminants stable; headline-worthy; the generated JS event table carries it), text naming the basin, the metres above or below its founding mark and the years since a stand this extreme. Basins are named at dawn from a stream of their own (a named lake feature's name where one sits on the water; else coined in the nearest tongue within `TONGUE_REACH`, the Old Tongue beyond it; "Mere"/"Lake" styles kept) and each coined basin joins the cast as a `Feature` entity so its lines link to a place; geometry is built pre-widen from the filled elevations and D8 directions the hydrology stage still holds and rides `widen` with the interior. `Lakes::hash` (levels, records, terraces) joins `hash_state`; `lakes` joins the access table (K·C — no die, no Peoples, parallel-safe). Gates in the hydro lane ×3 seeds at 240 y, all against live state: every terminal basin named and unique (14 · 21 · 8 basins); catchments land and disjoint (0 overlapping · 0 wet cells · 0 without land); levels finite; worst-basin lag of level behind the catchment's wet/dry year 0 y ×3 (M93 gate: under two years); worst-basin tracking r 0.78 · 0.75 · 0.84; median swing 0.34 · 0.24 · 0.34 m (playa lakes breathe decimetres to metres); 0.31 · 0.25 · 0.36 strandlines per basin-century (9 · 11 · 6 dated lines); every line dated and anchored (0 undated), never duplicated (ledger = chronicle to the count, 0 dup), records stepped ≥0.30 m apart in date order with none inside the burn-in. Calibration honest: the first draft (τ 1.0–1.6 y, gain 1.5) left 2 lines across 14 basins in 240 y — a shore nobody would name — and was moved to the cited elasticity, not tuned to the gate. One performance correction made before closure, not after: the first closure run (16:32–17:29 UTC) composed at 1520·37·4, the four FAILs being tick rate (94 / 49 mo/s) and its two composition consequences — the system-lattice profiler put the lakes pass at 81% of tick time because it read the full-grid year sky (`with_year_sky`, a whole map's anomaly + catchment smoothing per call, a diagnostics path); moved to the pointwise `year_anomaly_at` over the sampled catchment, the pass fell 4042 → 18 ms per 40 y (1.9% of tick) with every M93 row unchanged in verdict, and that run was discarded for banking. Banked from one uninterrupted closure (build → wasm-audit → full suite, 17:43–18:19 UTC): **1523 pass · 38 warn · 0 fail** (warn set is the standing family plus host-load timing — untouched dawn stages read 20–28% slower than the M92 run and the tick-rate rows moved with them, 511 / 100 mo/s against hard floors of 200 / 100; nothing M93-borne), era gate SEALED with the suite composing clean, replay identity at era scale identical, generation/simulation/chunking reproducible with the yearly pass live, deep-earth layers agree native↔wasm. Shipped wasm 2 870 730 B (2.73 MiB, inside the E6.4 budget), stamp `6d8139b57cee`. |
| M94 | The Dry Edge | 2026-09-04 | The steppe now walks, and the wells now sink, on the composed sky — through the pastoral line the founding law already draws (`ARID_PRECIP_MM` 300, M55) and the anomaly law the harvests read, no new dial. New `dryedge.rs` ledger: the *edge* is every rain-fed land cell (no ice, tundra, desert or water) whose dawn rain sits within reach of the line (300–`EDGE_CEIL_MM` 460 mm — 5723 · 4378 cells, 4.9 · 3.7% of land), grouped into 8-connected *reaches* (480 · 508; 73 · 62 of ≥`EVENT_MIN_CELLS` 12) named at dawn from a stream of their own (Grazing/Downs/Marches style off the nearest named feature, coined in the nearest tongue, unique per world); the *groves* are the M55 oases (257 · 243). Law, per cell per year off the pointwise `year_anomaly_at`: `ENCROACH_YEARS` 5 dry years running (< 300 mm) and the steppe takes the cell; `RECOVER_YEARS` 3 years at or over `RECOVER_MM` 330 (a tenth over the line — the Sahel’s cover trailed the 1990s rains by years) and the grass returns; between the two lines nothing advances (hysteresis, not a toggle). A grove fails when its 8-year mean anomaly (`OASIS_MEMORY_YEARS`) draws the table below the `OASIS_MOUND_M` 15 m recharge mound by more than `OASIS_HYSTERESIS_M` 1 m *and* `OASIS_DRY_MAJORITY` 5 of the 8 years were dry (one anomalous year cannot kill a well; found and fixed on the first full-size probe, where a single deep year dominated the mean), and is watered again on the mirror. The ledger is primed at dawn from a prehistory — 4 years of edge anomaly, 8 of grove anomaly — so year 1 is not a false spring: clocks start where the sky left them, a grove already dead on the eve of the dawn is dead at it, silently, its episode dated to the year before. Speech at extent thresholds only: a reach speaks its onset when ≥12 cells are under steppe, a widening when the taken share crosses the next band, a return when it clears; groves speak failure and return; new `EventKind::Encroach` / `Regreen` (appended after `Strandline`, prior discriminants stable; headline-worthy; the generated JS table carries them), text naming the reach or grove, the years run dry, the leagues of pasture lost or the deficit in every hundred drops. Yearly `DryEdgeSystem` (month 11, after `Lakes`; `last_year` guards chunked ticks); `DryEdge::hash` (cells, runs, taken mask, grove episodes) joins `hash_state`; `dry-edge` joins the access table (Y·C — no die, no Peoples, parallel-safe), 2.3% of tick in the lattice profile. Gates in the civ lane ×2 suite seeds at 150 y, every one re-derived against live state, not the ledger’s own word: every cell the steppe took had rain-fed grass at the dawn and its 5 dry years re-derive from the sky (695 · 364 takings · 0 broke the run · 0 arid at dawn · driest-year margin −0.0 mm — the law bites exactly at the line); every grove turn re-derives bit-for-bit on the 8-year mean with the dry majority (17 · 12 turns · 0 off the law · 0 single-year); a rewound twin driven off the seed’s sky alone reaches the live ledger’s law hash (equal ×2); every turn dated, map-anchored, cast-subjected, one per (ground, year, kind), ledger = chronicle to the count (46 · 27 spoken, 0 undated · 0 unsubjected · 0 duplicate); takings 8.10 · 5.54 per 100 edge cells per century (the Sahel lost ~10% of its pasture over 1968–85); steppe holds 23.7 · 28.7 y mean over 554 · 240 releases (longer than the recovery clock — degraded ground waits for a wet run, not a wet year); turns 30.7 · 18.7 per century (the 777 figure counted the one pre-dawn grove; spoken 27 → 18.0; the prose line and rate now count only what the run itself dated — a display correction in the tree for the next suite, no band verdict moves). Seed 90210, not a civ suite lane, was probed direct at 150 y: all M94 rows pass; its two FAILs are the standing M2.6/M92 famine family, pre-existing — the 12345/777 growth, famine and named-drought figures are byte-identical to the banked M93 civ reports, so M94 feeds nothing back. Decisions recorded, made before the closure run, not after: `EVENT_MIN_CELLS` 4 → 12 (four cells of steppe is not a chronicle line); the turn-rate band was first drafted narrower and warned at 31.3 on 12345 — re-derived to the M80 named-drought sibling (40 sweet · 90 hard ceilings, the same generational-cadence rationale), lower bound opened to 0.5 for thin-edged worlds; dawn priming independently moved 12345 to 30.7. Two gate fixes were arithmetic on the harness, not the law: the twin first lacked prehistory (rewound ledgers now reprime from the same forcing), and the ledger-vs-chronicle count charged the chronicle for the pre-dawn grove it was never owed (both seeds now equal to the count). Banked from one uninterrupted closure (build → wasm-audit → full suite, 20:30–21:10 UTC): **1540 pass · 37 warn · 0 fail** (warn set is the standing family plus host-load timing, per-lane warn counts unchanged from M93: civ 5 · 6; tick-rate rows 558 / 116 mo/s against hard floors of 200 / 100; nothing M94-borne), era gate SEALED with the suite composing clean, replay identity at era scale identical, generation/simulation/chunking reproducible with the yearly pass live, deep-earth layers agree native↔wasm. Shipped wasm 2 886 843 B (2.75 MiB, +16.1 KB for the ledger and its speech, inside the E6.4 3.0 MiB sweet band), Brotli wire 863 654 B, stamp `af4e327c4a7b`. |
| M95 | Hunger With a Cause | 2026-09-04 | Every famine now carries the sky it read, and says so. `famine::HarvestSky` is the composite law (M71 base sky + M74 oscillation + M83 drift + M84 belts, through the M72 SPI memory) read at that cell-year: this year's SPI `z1`, the raw rain anomaly `dp`, the 12-year memory `index`, the dry and lean runs, the failed years behind a carried famine; `HarvestVerdict` is that sky through the rain-fed law (or the M92 monsoon index through the paddy law — cell sky for open paddies, the M81 catchment sky for riverine ones) to a shortfall, and the toll is the shortfall's own arithmetic (`famine::toll`, `pop·(0.05+0.16·sf)·granary`, shared by the pass and the twin). One law, three readers: the month-7 `famine_pass` records the verdict on every `FamineRow` and captures the spoken text; `famine::cause_sentence` turns the sky into the chronicle's clause (dry years running and the year's shortfall, the failed years behind a carried famine, or the monsoon's share) and the famine line carries it verbatim, patina suffix or none; `explain::harvest_sky_block` puts the same verdict on the settlement card as a *Sky · Y n* block (tense turns at the verdict month — the harvest ahead, the harvest past) rendered by `inspector.js` (`.insp-sky`, amber when the year fails). No new state, nothing packed, nothing hashed: the sky is a pure function of seed × cell × year. Gates in the civ lane ×2 suite seeds at 150 y, every one re-derived independently of the ledger's own word: every famine's sky re-derives bit-equal from the seed (343/343 · 156/156 rows: SPI, anomaly, memory, runs → shortfall → toll, no residual); every famine tells its cause (343/343 · 156/156 tellings contain the sentence their re-derived sky makes, 0 untold); the famine's sky is the climate lane's sky (343/343 · 156/156 rows equal the full-grid `year_anomaly` at their cell-year, bit for bit — the pointwise law and the grid law had to be made to multiply and divide in the same order, found on the first probe and fixed in the grid path, not the tolerance); the inspector reads the same sky (104/104 · 105/105 explain blocks agree with the law at the current year); the M72 year-not-place and shortfall-arithmetic rows hold unchanged. Sweep lane, 100 y × 5 seeds: 749 famines, 749 traced bit-equal, 749 tell their cause (12345 154 · 777 79 · 31337 112 · 90210 296 · 555 108) — no seed, no row excepted. The closure was not accepted on the first full run: 1547 pass · 36 warn · **3 fail**, all three one finding — E10.2 year-100 tick rate 97 mo/s on seed 777 against the hard floor of 100, cascading into the suite and Era-I rows. Root cause, established by an interleaved A/B against the last pre-M95 tree on the same host (pre 100 · 97 mo/s, post 102 · 99 mo/s): not M95 — seed 777 had been sitting on the floor since the M92 paddies began reading the catchment sky (`catchment_anomaly_at`: 2,401 taps per read, each tap a two-lane fbm with the row terms recomputed; `systems` profile 6073 ms/100 y — towns 36.9%, famine 14.2%, colonize 13.5%). The floor was not touched; the reader was: `climate::RowSky` hoists the per-row amplitude/teleconnection/belt terms once per row, `rain_anomaly_at` evaluates the rain lane alone (the temperature lane was computed and discarded at every tap), `World::catchment_rain_anomaly` reads the active-year cache first and the raw kernel only for history, `monsoon_of` is the one monsoon arithmetic for point, catchment and `World::monsoon_index`. Behaviour-neutral by construction and proved so: the 120-mo determinism hashes are byte-identical to the banked reports (gen `3478a920606bd716`, state `d4901c634e7d056b`, events `f3c4e9b824e374ba`, towns `ada237d707f845c3`), the 150-y civ reports for 12345 and 777 diff clean against the banked ones with timing lines filtered, and a new hard row proves the per-site kernel bit-equal to the full `gaussian_filter` at every town (307/307 across the three perf seeds). `diagnose perf` gained a *sky-read anatomy* with four bands re-derived from the measured optimized run: catchment read ≤400 µs sweet / 1000 hard (139 worst; the replaced kernel would trip the hard line), point rain read ≤600 ns / 2000 (380), cold yearly yields ≤20 ms / 60 (14.3), warm verdicts ≤5 ms / 30 (1.0). Seed 777 year-100: 97 → 145 mo/s; worst-seed year-0 750. Banked from one uninterrupted closure (build → wasm-audit → full suite, 22:55–23:30 UTC): **1552 pass · 39 warn · 0 fail** (warn set identical by name to the last banked summary; civ-lane warn counts unchanged at 5 · 6; the tick-rate rows now 750 / 145 mo/s against hard floors of 200 / 100; nothing M95-borne), era gate SEALED with the suite composing clean, replay identity at era scale identical, generation/simulation/chunking reproducible, deep-earth layers agree native↔wasm. Shipped wasm 2 895 129 B (2.76 MiB, +8.3 KB for the verdict, its sentence and the card block, inside the E6.4 3.0 MiB sweet band), Brotli wire 866 623 B, stamp `1eaf366d95f7`. |
| M96 | Granaries Against Lean Years | 2026-09-05 | The Pottery discount is gone; in its place the town keeps grain. `society::StoreTier` is the craft's own ladder — *none* (no craft) · *jars* (Pottery) · *granaries* (Masonry) · *storehouses* (Law) — each a share of the fat year laid by (0.35 · 0.60 · 0.85), a spoilage per year (0.30 · 0.15 · 0.08) and a roof in population-years (0.25 · 0.75 · 1.5); the tier is derived from the tech set at every read, never stored. The store is per-settlement state (`Settlement::store`, `store_year`, hashed in `hash_state`, packed to nothing — the card reads it through `explain`). Law, once a year before the month-7 verdicts (`famine::granary_turn`, idempotent under chunked ticks by `store_year`): spoil what is kept, add `share · (yield − 1)⁺ · pop`, cap at the roof, round to a thousandth of a person-year; at a lean verdict `store_draw` covers up to `shortfall · pop` and the toll is the *uncovered* fraction through the M72 arithmetic — a full store pays no toll, an empty one pays the bare law. A year the store took without a death is spoken once as a new `EventKind::Granary` (appended after `Regreen`, prior discriminants stable, `granary` in the generated JS table, gold in the HUD); a famine that drew and still killed carries the gave-clause in its M95 sentence. The card gains a *Store* block (tier, months of grain in hand, this year's need, the share/spoil/roof, the last fill) rendered by `inspector.js` as a gauge. Calibration was measured, not guessed: over 10 419 storing town-years per suite seed (150 y) 47–48% of years give more than the town eats, the mean yield sits at 0.99–1.00, the surplus averages 0.037–0.043 pop-years a year (0.078–0.093 over the fat years), so the storehouse steady state on that surplus is 0.40–0.46 pop-years ≈ 4.8–5.5 months against a 1.5-year roof; the first draft (shares 0.10/0.20/0.30) left a 0.3-month median at the verdict and was resized from this table, and the `fat years` diagnostic that produced it stays in the report. Gates in the civ lane ×2 suite seeds at 150 y, every row re-derived independently of the ledger's own word: the store is the law's own arithmetic (316/316 · 195/195 lean verdicts re-derive draw, granary share, paid toll, bare toll and spoken state exactly, all under the roof); untiered towns lay nothing by (0 · 0 such verdicts on these seeds — the sweep supplies them); spoken famines carry their store (237/237 · 157/157 tied to a famine row with the same store, gave-clause where it gave); held years are told once (79/79 · 38/38 Granary events, 0 orphans); storage tiers reduce the toll (38.4% · 48.4% of the bare toll kept alive over the tiered verdicts); the higher craft keeps more (surviving share monotone down the ladder); matched shortfalls favour the storing town (four shortfall quartiles with ≥10 verdicts each, per-head toll as paid below the same rows' bare toll in every one: 0.021<0.067 · 0.061<0.110 · 0.112<0.167 · 0.162<0.209 on 12345); the store holds seasons (storehouses: median 2.0 · 1.9 months of grain *entering* a lean run, p90 8.6 · 7.4, n 254 · 115); the inspector reads the same store (102/102 · 101/101 explain blocks agree with the law at the current year). Sweep lane, 100 y × 5 seeds: 664 lean verdicts, 664 exact, roofed, tied and clause-true; 98/98 held years told; 30.7% of the bare toll kept over 655 tiered verdicts; and the ladder holds across the ensemble with a real bare rung under it — no store 9 verdicts 0.0% kept · jars 87 verdicts 8.3% · granaries 6 verdicts 24.9% · storehouses 562 verdicts 31.4%. Two harness decisions recorded: the matched-shortfall row was first drafted tiered-vs-untiered and was vacuous on the suite seeds (every town had at least jars by its first lean year), so it compares each tiered row's paid toll with that same row's bare counterfactual, quartile by quartile — the matched control is the town itself; and the months-in-hand band was first read over every verdict and sat near zero not because the store was small but because a multi-year drought drains it — the row now measures the store entering a lean run (band 1–12 months sweet, 0.3–24 hard, ≥10 rows) and reports the deep-in-run figure beside it (0.0 · 0.0, by design). On seed 12345 the rain-fed famine cadence fell 134.7 → 83.3 per century: the same skies, fewer deaths spoken. Banked from one uninterrupted closure (build → wasm-audit → full suite, 00:21–00:56 UTC): **1575 pass · 38 warn · 0 fail** (civ-lane warn counts unchanged at 5 · 6; the by-name set moves only where the world moved or the host did: gained a second assimilation-cadence row on 12345 at 0.7 flips/century, the recurring M14.5 kilns row on economy-12345 and a host-timing point-rain-read row at 627 ns worst seed against a 600 sweet line; lost stage-naming timing, one dry-frontier veto row and the gate's veto-bites want, which now passes with 1 refused town; the tick-rate rows 727 / 151 mo/s against hard floors of 200 / 100; nothing M96-borne fails), era gate SEALED with the suite composing clean, replay identity at era scale identical, generation/simulation/chunking reproducible with the store live, deep-earth layers agree native↔wasm. Shipped wasm 2 902 170 B (2.77 MiB, +7.0 KB for the store, its speech and the card block, inside the E6.4 3.0 MiB sweet band), Brotli wire 868 106 B, stamp `2713d9863f6d`. |
| M97 | Famine, Recalibrated | 2026-09-05 | The hunger cadence is now set against the record, not the gut, and the record's first lesson was that the law spoke too often. Through M96 the toll was `pop × (0.05 + 0.16·shortfall) × granary` — a twentieth of the town struck the moment the SPI memory crossed −1, whatever the depth — so every failed harvest was a famine and seed 12345 ran 16.1 spoken per settlement-century over 150 y, Hoskins' harvest-*failure* rate (one in six) wearing famine's name, against a record whose famine counts top out near ten (Farr) and centre near one to five (Campbell & Ó Gráda's England ~1, Mallory's China ~5 per province). The record keeps dearth and famine apart — a bad harvest with high prices and tightened belts is common; people die only where the net want ran to a quarter–third of a year's bread, almost always back-to-back — so M97 puts that floor in the law: `famine::toll` reads the want the store left (`shortfall × granary`, M96's own factor), is exactly zero under `TOLL_FLOOR` 0.5 of a year's grain, and climbs linearly to `TOLL_RATE` of the town at a whole year's want; `DEAD_SHARE` 0.55 of the struck die, the rest walk. Below the floor the year is a *dearth*: the store still draws, grain still spikes, the chronicle stays quiet. The rate was set from the record's own unit, the *episode* — Ó Gráda's percentages are per famine (1315–17 is one entry, three harvests long) — so the harness merges consecutive famine years at a town and reads the summed dead share: at the old law's saturation (a fifth struck, 11.5% dead per year) the measured episode ran 10–12% dead on every seed, i.e. the mean famine was England's Great Famine, the worst line in the ledger; `TOLL_RATE` 0.15 (saturating year 8.25%) lands the episode in the ledger's middle. The exposure census is the pass's own predicate (every eligible settlement-year counted, 0 famine rows outside it, on every seed) so the rate's denominator is not guessed; the M82 return-time instrument extends to `FamineCadence` (spoken · bare-law · failed-harvest rates per settlement-century, dead share per famine year and per episode, the storage tier's own band on its own town-years). New bands in `famine::BANDS` (`FAMINE_PER_CENTURY` 1–12 sweet · 0.3–20 hard, `STORED_FAMINE_PER_CENTURY` 0.3–8 · 0–14, `FAMINE_DEAD_SHARE` 2–8% · 1–15%, `FAMINE_EPISODE_DEAD_SHARE` 3–10% · 1–16%); the M2.6 world-total rain-fed cadence gate is retired in their favour. Every reader was re-derived to the one law: the M72 dose regression now fits per-capita toll against `want_of` and holds the fitted slope/intercept to the law's own (measured −0.1498+0.2991·want vs −0.150+0.300, ρ 0.9997 on 12345; −0.1505+0.2994 on 777), the M96 matched-quartile control draws only rows where the bare law would strike (no zero-against-zero quartiles), `explain.rs` and the settlement card distinguish famine (red) · held (the store covered it) · dearth (amber, the sentence naming the shortfall, the want the store left and the 50% floor) — verified live in the browser at Y12: Zarimsif's paddies at 0% monsoon render `insp-sky fail` (37 souls), Alimmasab's 15% shortfall renders `insp-sky lean` with the dearth sentence. Measured at closure — civ 12345 150 y: 5.4 spoken/c over 26.1 settlement-centuries (13.6 failed harvests/c beneath it), 5.2% dead per famine year, 6.7% per episode (110 episodes, longest 5 y), storehouses 5.4/c vs the bare law's 6.8/c on the same town-years; civ 777: 1.3/c, 5.9%, 9.1% per episode, storehouses 1.3/c vs 1.8/c; five-seed sweep 100 y: **5/5 seeds inside the envelope** (12345 5.2/c · 777 1.6/c · 31337 6.3/c · 90210 8.5/c · 555 3.7/c; dead 5.0–5.8%/famine year, 5.8–9.2% per episode), pooled 5.0 spoken/c against 6.1/c bare law and 10.9 failed harvests/c over 71.3 settlement-centuries, 280 episodes at 6.8% dead, the granary tier under the bare law on every banded seed. M80 carry-over holds (18–22%). Banked from one uninterrupted closure (build → wasm-audit → full suite, 01:46–02:21 UTC): **1591 pass · 37 warn · 0 fail** (by-name warn set vs M96: lost both M2.6 famine-cadence rows (retired), the kilns row, the point-rain-read timing row and the post-y100 ruins row — fewer hunger-hollowed towns fall, and the row passes; gained `median town spacing` on both civ seeds at 48 / 53 km (was 34 / 38; sweet ≤48, hard ≤120, town count 105 / 102 unchanged — the +25% population on 12345 pushes colonies past the crowding term into the frontier; staged below as a measurement item, since the row's target says *settled cores* and the median reads every town) and the M55 veto want on both seeds (0 counterfactual dry foundings, the standing want since M81); tick-rate rows 828 / 149 mo/s against hard floors 200 / 100; nothing M97-borne fails), era gate SEALED with the suite composing clean, replay identity at era scale identical, generation/simulation/chunking reproducible under the new law, deep-earth layers agree native↔wasm. Shipped wasm 2 903 382 B (2.77 MiB, +1.2 KB for the floor law and the dearth sentence, inside the E6.4 3.0 MiB sweet band), Brotli wire 868 386 B, stamp `a8bbb22c6d8e`. |
| M98 | Off the Failing Margin | 2026-09-05 | civ+sweep lanes, `migration.rs` — a ten-harvest settlement memory of the M95 sky anomaly + M90 abandonment (`Roads`, rounded at the law boundary, kept for dead towns); `FAILED_DECADE=0.25` over a full ring → one deterministic same-culture pulse of 15–40% (`leave_share`) to the nearest kin-town whose own decade mean ≥`REFUGE_MEAN=0.15` for ≥3 readings, arriving the same month, one `Exodus` event per pulse, town armed again after ten years; eligibility is one shared predicate `migration::hold` (`Walked`/`Small`/`Famine`/`Unspoken`) read by both the tick pass and `explain::roads` so the inspector never promises a road the law would hold. Sweep ×5 seeds ×300 y: 104 pulses · 0 no-refuge · 0.46/settlement-century · share 24.6% · road 133 km, every pulse re-derived from the ledger; civ 12345/777: 38 pulses each, failed-decade law, same-month arrival, every failed decade answered, event↔pulse identity all green. Full suite on the banked bytes: 1611 pass · 38 warn · 0 fail (warns unchanged in kind: gen total, coast-law CPU, stage naming, ruins rate — all inside hard); Era I gate SEALED; browser 6 pass · 1 warn · 0 fail; wasm 2,924,220 B · brotli 873,443 B · stamp `5332690e303e` |
